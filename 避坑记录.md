# 避坑记录

用于记录已经证伪的路径、命令和判断，避免后续会话重复踩坑。

## Collector / Source
- `csrc`
  - 不要再用 `https://www.csrc.gov.cn/csrc/c100028/common_xq_list.shtml` 作为抓取入口；当前返回空列表。
  - 当前更稳的入口是证监会首页 `新闻发布 -> 证监会要闻` 模块。
- `sse`
  - 在 `queryCompanyBulletinNew.do` 参数未完全验通前，不要把 `sse` 打开到 `--source all`。
  - 当前 `enabled: false` 是有意 staged，不是漏配。
- `szse`
  - 官方公告查询更稳的入口是 `detailinfo?plateCode=szse&pageSize=50&pageNum=1` 这一类官方明细接口，不要先凭经验猜 `annList` 参数。
  - `szse` 已经启用；后续不要再把它当 staged 源重复验通。
- `hkex`
  - 不要直接抓 `lci.html` 的表格 HTML；那一层更像外壳，不是稳定数据源。
  - 当前更稳的路径是先读官方脚本 `lci.js`，再取其指向的 `/ncms/json/eds/` JSON 分页。
  - `hkex` 启用前不要直接并入 `--source all`；先补最小过滤，尤其是 `Next Day Disclosure Return / AGM / Proxy / Annual / ESG / Monthly Return / Board Meeting` 这些高噪音标题族。

## Workflow
- 主仓库根目录的 `progress.md / task_plan.md / findings.md` 明显落后于 worktree；接手时优先看 `.worktrees/mvp-foundation`。
- `data/events/events.jsonl` 可能是旧快照，不能单独拿来证明当前规则仍失效；优先以现跑结果和 `live-smoke` 为准。
- `live-smoke --source all` 如果被用户手动中断，不能写成“已验收通过”。
- `report` 过滤和 `audit-suspicious` 是两套逻辑；不要因为 report 头部干净，就默认 `audit-suspicious` 也会同步归零。
- live 样本头部会随日期滚动切换；不要沿用上一轮头部清单直接继续下规则，必须先重跑 `live-smoke --source all`。
- 连续清掉交易所材料噪音后，如果头部剩下的是 `control_change / order_contract` 这类真正催化候选，先判断是否要修 `event_subtype / analysis`，不要继续在 report 层硬压。
- `cls` 现在是主链全球快讯源；不要再按旧的 A 股单线目标默认压掉海外快讯、栏目稿或全球 roundup。
- `general_fast_news` 已经和 `market_move` 拆档；后续如果看到 `cls` 栏目稿在头部，优先考虑展示分层，不要把它重新抬回 `market_move` 同档强度。
- `miit` 无题材会务标题不只会写成 `座谈会 / 会见 / 新闻发布会`，也会写成 `总体组 / 咨询组 / 全体会议`，继续回流时先补会务过滤，不要误判成新题材政策。
- `cls industry_data / general_fast_news` 如果头部靠前，优先做排序降层；不要为了干净头部直接删除全球资讯内容。
- `【公告全知道】` 不是纯编辑尾噪；它经常包装真实催化摘要，继续下刀前先确认是否会误伤真事件。
- `华测导航：开展供应链融资业务合作暨对外担保` 这类标题不要直接并入“授信/担保额度材料”低信号簇；当前更像弱簇边界，先补正反样本再决定是否压。
- `再次向港交所提交上市申请书` 的低信号快讯不只会出现在 `stcn`，`cls` 也会出现；过滤不要绑死单一 source。
- `盘后A股上市公司重点业绩公告精选` 这类 digest 回归不要用占位摘要替代真实文本，否则容易把“栏目包装稿”和“真实业绩催化摘要”混在一起。
- `国内期货夜盘收盘多数上涨 ...` 属于已知 `market_move roundup` 同簇变体；优先补 `text_report` 词表，不要误判成 `event_merge` 或 `scoring` 问题。
- `申请撤销对公司股票交易实施退市风险警示` 这种表述虽然带 `风险`，但语义上属于风险撤销申请；先补 bullish 白名单，不要被通用 `风险` 词误打成 `bearish`。
- 推送前不要假定远端仓库已存在；先 `gh repo view owner/repo`，不存在再 `gh repo create owner/repo ...`。

## Scope
- 当前主线是 `live` 样本收口和第一批官方源扩展，不要顺手扩宽题材库。
- 社交/自媒体即使后续接入，也应先作为线索层，不能直接进主信号层。
- `冻结 / 解除冻结` 这类标题不能自动按低信号材料处理；先判断是程序性披露，还是实质风险状态变化。
- `申请破产清算` 不能继续留在 `corporate_disclosure / neutral`；这类样本应优先回正到 `reorganization_risk / bearish`。
