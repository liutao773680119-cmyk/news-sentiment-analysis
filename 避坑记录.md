# 避坑记录

用于记录已经证伪的路径、命令和判断，避免后续会话重复踩坑。

## Collector / Source
- `csrc`
  - 不要再用 `https://www.csrc.gov.cn/csrc/c100028/common_xq_list.shtml` 作为抓取入口；当前返回空列表。
  - 当前更稳的入口是证监会首页 `新闻发布 -> 证监会要闻` 模块。
- `sse`
  - 在 `queryCompanyBulletinNew.do` 参数未完全验通前，不要把 `sse` 打开到 `--source all`。
  - 当前 `enabled: false` 是有意 staged，不是漏配。
- `szse`
  - 官方公告查询更稳的入口是 `detailinfo?plateCode=szse&pageSize=50&pageNum=1` 这一类官方明细接口，不要先凭经验猜 `annList` 参数。
  - `szse` 已经启用；后续不要再把它当 staged 源重复验通。
- `hkex`
  - 不要直接抓 `lci.html` 的表格 HTML；那一层更像外壳，不是稳定数据源。
  - 当前更稳的路径是先读官方脚本 `lci.js`，再取其指向的 `/ncms/json/eds/` JSON 分页。
  - `hkex` 启用前不要直接并入 `--source all`；先补最小过滤，尤其是 `Next Day Disclosure Return / AGM / Proxy / Annual / ESG / Monthly Return / Board Meeting` 这些高噪音标题族。
  - `hkex` 英文标题不要走通用字符重合归并；否则 `LIST OF DIRECTORS ...` 和 `PROFIT WARNING` 这种完全不相干的标题也会被错并。
  - `hkex` 英文材料标题过滤要做大小写无关匹配；真实样本里全大写和普通大小写会混着出现。
  - `PROFIT WARNING / POSITIVE PROFIT ALERT` 需要单独方向词处理；默认 `neutral` 会把真实风险/利好信号压平。
  - `INSIDE INFORMATION` 过宽，不要整体当成单一 subtype；先按 `winding up / arbitration` 这类窄上下文拆。
  - 继续收 `hkex` 时，不要直接把 `CONNECTED / DISCLOSEABLE / MAJOR TRANSACTION` 当低信号；优先盯 `AGM/EGM`、`delay in despatch`、`supplemental announcement`、`framework agreement`、`lease agreement`、`results + continued suspension` 这类材料尾巴。
  - 当 `hkex` 头部只剩 `CONNECTED TRANSACTION - ACQUISITION OF SOFTWARE ASSETS`、`CAPITAL INCREASE AGREEMENT`、`FURTHER INVESTMENT IN PRECIOUS METALS` 这类标题时，继续下刀的误伤风险已经很高，先停手。
- `run-once --source hkex` 在当前环境里经常几乎没有 stdout；不要拿“终端很安静”当失败，先直接看 `latest_report.txt` 和 `audit-suspicious`。
- `weibo`
  - 当前真实抓取入口仍会遇到 visitor gate / 403；不要把 `collect-social --platform weibo` 能优雅失败，误写成“微博已生产可用”。
  - 现阶段 `weibo` 只保留最小 sidecar 接线和失败提示；继续推进前先确认是否有稳定公开入口。

## Workflow
- `创业板指、深证成指均涨逾2% 半导体、券商等板块活跃` 这类 `A股指数 + 板块活跃` 样本，本质更像 `market_reference`，不是新的内容异常；但只能在同时具备 `指数关键词 + 板块活跃 + 涨幅热度词 + 已有题材映射` 时按最窄口径放过。
- 不要把所有 `指数上涨 / 翻红 / 震荡 / 收评` 一刀切放进 `market_reference`；纯指数综述、点位播报仍要谨慎看待。
- 手动 `audit-suspicious=0` 不等于后台已经吃到新规则；必须继续等自然下一轮 `watchdog_status=clean`，再写“已恢复”。
- `中国高科...股价波动事项的问询函` 这类交易所问询材料当前按 audit 材料噪声处理；不要把它扩大成所有 `问询函` 都降噪，尤其不要误伤 `立案 / 处罚 / 退市风险 / 重大诉讼` 真风险公告。
- `审核问询函有关财务事项的说明` 与 `审核问询函中有关财务事项的说明` 是同族融资审核材料文案；audit 和 text_report 两侧要同步补词，不能只修一边。
- watchdog 6 小时汇总只用于减少人工盯日志，不自动改规则、不自动提交；新 suspicious 仍要人工确认后固化。
- 6 小时汇总里的 `Report Highlights` 是当前 `latest_report.txt` 的前 10 条摘要入口，不是完整报告，也不是 6 小时内每轮报告的历史回放。
- 用户平时不看 `latest_report.txt` 时，优先打开最新 `data/monitoring/summaries/*-summary.md` 的 `Report Highlights`；不要继续只看旧的 `Latest Report Head`。
- `股东所持部分股份冻结` 当前按材料型冻结公告 audit 尾噪处理；不要扩成所有 `冻结` 公告都降噪，尤其不要误伤公司资产被查封、被司法冻结、被执行、立案调查等真实风险。
- `suspicious_count=0` 但 `watchdog_status=alert` 时，先看 `failed_sources`；这通常是采集源失败，不要误当内容异常继续补规则。
- 私营生物医药 C 轮融资泛稿只在缺少上市公司、政策、订单、产业链兑现路径时按 audit 尾噪处理；不要扩成“所有融资新闻都是噪声”。
- `Takeda + antitrust/lawsuit/generic drug` 这类海外药企法务新闻，本轮是 `半导体` 主题串味；只按 `investing_news` 窄条件跳过 audit，不要扩成“所有海外医药新闻都降噪”。
- `【淘金互动易】` 算力产业链内容如果有机构观点、公司布局、明确应用路径，按 `market_reference` 保留报告，只从后台异常口径退出；不要当普通低信号删除。
- 会见/交流类快讯只有在没有 `签署 / 中标 / 订单 / 合同 / 采购` 等落地词时按尾噪处理；出现落地词时不能套用。
- `江苏省委书记会见 AMD / 何立峰会见 AMD / 外汇局会见友邦保险` 这类会见交流快讯可按 audit 尾噪处理；但一旦标题或正文出现签约、订单、中标、采购、合同，不能套用。
- `现货黄金/现货白银` 的点位和涨跌幅播报可按 audit 尾噪处理；不要扩到黄金产业链、央行购金、矿业公司或政策类新闻。
- 年报问询函审计专项说明、法律意见书是材料尾噪；但真实退市、撤销风险警示、重大诉讼/仲裁风险不能套用。
- 主仓库根目录的 `progress.md / task_plan.md / findings.md` 明显落后于 worktree；接手时优先看 `.worktrees/mvp-foundation`。
- `data/events/events.jsonl` 可能是旧快照，不能单独拿来证明当前规则仍失效；优先以现跑结果和 `live-smoke` 为准。
- `live-smoke --source all` 如果被用户手动中断，不能写成“已验收通过”。
- `cninfo` 这类年报问询函/发行优先股/新增诉讼材料处理优先级：
  - 先按最窄 title-only/no-reply 口径补白名单，不扩到全局问询函。
  - 问询函风格有多版本：`问询函》回复`、`监管问询函的回复`、`年报问询函之回复`、`年报有关事项问询函` 等都要覆盖。
  - `新增诉讼及进展情况` 与 `累计新增诉讼` 要区分，避免抹掉真实诉讼进展类保留样本。
  - 在 `audit-suspicious` 与 `latest_report.txt` 均验证无命中，再考虑写入长期规则。
- `report` 过滤和 `audit-suspicious` 是两套逻辑；不要因为 report 头部干净，就默认 `audit-suspicious` 也会同步归零。
- `report` 收掉 live 头部弱样本后，必须重跑 `audit-suspicious`；2026-04-29 已出现 report 目标退出后，新的 `累计新增诉讼` 和 `Pre-A 融资机器人泛稿` 仍被 audit 命中的情况。
- `audit-suspicious` 豁免不能只补 CLI；如果样本也会出现在 report 里，必须同步补 `text_report` 红灯测试和过滤规则。
- `audit-suspicious=0` 不代表 report 头部一定干净；2026-04-28 已出现 `audit=0` 但 report 头部仍有 `irm_cninfo` 弱问答和年报季材料的情况。
- live 样本头部会随日期滚动切换；不要沿用上一轮头部清单直接继续下规则，必须先重跑 `live-smoke --source all`。
- 连续清掉交易所材料噪音后，如果头部剩下的是 `control_change / order_contract` 这类真正催化候选，先判断是否要修 `event_subtype / analysis`，不要继续在 report 层硬压。
- `cls` 现在是主链全球快讯源；不要再按旧的 A 股单线目标默认压掉海外快讯、栏目稿或全球 roundup。
- `general_fast_news` 已经和 `market_move` 拆档；后续如果看到 `cls` 栏目稿在头部，优先考虑展示分层，不要把它重新抬回 `market_move` 同档强度。
- `report` 分层骨架已经落地后，下一轮优先看“层内排序”而不是回到单榜继续压头部。
- `miit` 无题材会务标题不只会写成 `座谈会 / 会见 / 新闻发布会`，也会写成 `总体组 / 咨询组 / 全体会议`，继续回流时先补会务过滤，不要误判成新题材政策。
- `cls industry_data / general_fast_news` 如果头部靠前，优先做排序降层；不要为了干净头部直接删除全球资讯内容。
- 当前 `--source all` 主线已经是全球多源混合输入，不要再用旧 `phase8-live-boundary` 的 A股单线标准验收。
- `text_report` 时间窗不要用全部事件计算；低信号新材料也会抬高最新批次时间，导致旧正例被错误过滤。应先过 `_is_market_relevant` 再算 report cutoff。
- `cls/stcn market_move` 带题材不等于 A股强催化；A股板块/概念/涨停类可回 A股层，海外单股主题异动仍应留在 `全球市场与商品`。
- 年报季 `sse/szse` 材料会被题材映射抬高，例如工程机械/房地产；董事会决议、会计政策、减值、募集资金、ESG、审计履职、回购报告书等仍按材料口径过滤。
- 标准交接时要把 `global-multisource-mainline` 和 `social-sidecar` 分开写；不要把“展示分层”和“社交 sidecar”混成一个待办。
- `【公告全知道】` 不是纯编辑尾噪；它经常包装真实催化摘要，继续下刀前先确认是否会误伤真事件。
- `华测导航：开展供应链融资业务合作暨对外担保` 这类标题不要直接并入“授信/担保额度材料”低信号簇；当前更像弱簇边界，先补正反样本再决定是否压。
- `再次向港交所提交上市申请书` 的低信号快讯不只会出现在 `stcn`，`cls` 也会出现；过滤不要绑死单一 source。
- `盘后A股上市公司重点业绩公告精选` 这类 digest 回归不要用占位摘要替代真实文本，否则容易把“栏目包装稿”和“真实业绩催化摘要”混在一起。
- `国内期货夜盘收盘多数上涨 ...` 属于已知 `market_move roundup` 同簇变体；优先补 `text_report` 词表，不要误判成 `event_merge` 或 `scoring` 问题。
- `申请撤销对公司股票交易实施退市风险警示` 这种表述虽然带 `风险`，但语义上属于风险撤销申请；先补 bullish 白名单，不要被通用 `风险` 词误打成 `bearish`。
- `企查查APP显示 + 经营范围包含 + 股权穿透显示` 这类 `company_update` 更像工商注册口径；优先在 `analysis/rules.py` 收 summary theme spillover，不要先在 `report` 层硬压。
- `cls + general_fast_news + 全球首款/研发成功 + 科技日报 + 教授` 更像科技特稿，不要直接按公司催化或技术落地快讯保留。
- `股东协议 + 补充协议` 这类 `sse/szse corporate_disclosure` 先按协议材料处理；但含 `完成 / 获批 / 控制权变更 / 复牌` 等结果词时不要一刀切。
- `irm_cninfo` 如果标题是热点追问、回复只有回避口径、年报导向、占比很低、后续公告为准这类弱信息，优先在 `text_report` 收口，不要急着改 `event_subtype`。
- `irm_cninfo` 的 `营收/订单大概多少`、`股价与业绩不对称/新项目么`、`算力基建/后续布局` 这类问法，本质上还是同一类弱问答；优先继续在 `text_report` helper 里补最窄口径。
- `stcn` 的 `互动平台表示 + 暂未/未参股/未投资/未布局` 更像否定式回应，不要只因为标题里带 `CPO / 存储 / 算力` 就保留在头部。
- `stcn` 的券商评论稿、基金经理配置评论、行业景气综述和 `【早知道】` 摘要拼盘，经常会同时带主题词和高分；继续回流时优先补 `text_report` 过滤，并同步检查 `audit-suspicious` 白名单。
- `吉利 Robotaxi` 这类有明确主体、明确时间点、明确发布动作和技术细节的快讯，优先视为 legit 保留样本，不要按栏目稿或弱问答一刀切。
- `【财联社早知道】` 不是一刀切；`机构预估 / 分析师称 / 这家公司` 这类匿名拼盘稿可以过滤，但带明确个股催化的内容先别误伤。
- `海外单股并购快讯 + 无题材无个股` 不适合留在 A 股主报告头部；优先当弱快讯处理，不要抬成主催化。
- 头部已经回到 `退市风险 / 撤销风险警示` 这类 legit 风险公告时，继续追求“更干净”通常收益很小，容易过拟合。
- `live-smoke --source all` 在当前环境里可能出现“产物已刷新但 stdout 没有摘要回吐”；不要只盯终端，要同步看 `latest_report.txt` 和相关产物时间戳。
- 当前 analyses 真路径是 `data/events/event_analysis.jsonl`；不要去查不存在的 `data/analysis/analyses.jsonl`。
- live 重跑后，同一簇 `irm_cninfo` 样本的 `event_id` 可能变化；排障时先按当前标题、summary、member_news_ids 对齐，不要硬绑上一轮 `event_id`。
- `hard_event` 同模板交易所公告不能只靠标题相似度归并；像 `退市风险警示暨股票停牌/停复牌安排` 这类模板公告，必须先看股票代码，否则会把不同公司错并成一个 event。
- `重大诉讼进展 / 立案通知书` 这类只有标题词面的风险公告，不要继续留在 `corporate_disclosure / neutral`；当前更稳的是直接回正到 `legal_dispute / bearish`。
- 修完 `event_merge` 后不要只刷 `report` 就下结论；`events.jsonl` 只有在重新跑 `merge-events` 或完整链路后才会反映新归并结果。
- `irm_cninfo` 当前 live 样本不一定有回复正文；遇到 `summary` 只有问题本身的 question-only 样本，旧的“标题+回复口径”规则会天然失效，先考虑最窄 title-only 收口。
- `irm_cninfo` 的 `网传中标...是真的吗`、`光伏储能相关业务吗` 这类 question-only live 样本，先在 `_is_low_signal_irm_cninfo_investor_qa()` 的 no-reply 分支补 title-only 组合；不要先扩成泛化的“相关业务吗/是真的吗”全局过滤。
- `sse_einteractive` 的弱抱怨问答如果 reply 只是 `尊重并重视投资者反馈`、`股价受宏观环境和二级市场波动影响` 这类 canned reply，优先按 `title + reply` 最窄组合过滤；不要先碰 subtype 或上游 merge。
- `cls` 的 `股价“一”字跌停 + 最新回应` 这类样本，根因通常不在 `event_merge` 或 `analysis`；如果它是 `market_move + 无题材 + 回应解释口径`，优先在 `text_report` 做窄过滤，不要为这类噪音去改 subtype。
- `irm_cninfo` 当天 live 里还会出现另一类“有回复但本质仍是弱问答”的样本：如 `经营范围介绍`、`订单充裕 + 定期报告为准`、`无未披露事项`、`审慎论证`、`并购方向泛回复`、`小批量供货但收入占比较小`、`减持预披露规则追问`、`继续回购诉求`、`定增正常推进`。这类优先继续在 `text_report` helper 补最窄 title+reply 组合，不要急着改上游 subtype。
- `PYTHONPATH=src ./.venv/bin/python -m news_sentiment report` 在当前环境里不一定可靠地按最新源码重写 `latest_report.txt`；验收时要直接搜目标样本，必要时用 `PYTHONPATH=src` 直接调用 `write_text_report()` 强制刷新。
- `latest_report.txt` 的强制重写和读取不要并行执行；先写后读，否则很容易把旧内容误判成“新规则没生效”。
- `news-sentiment-watch` 的最新 log 块如果早于刚提交的 commit，不要据此判断规则失效；必须等下一轮 loop 自动吃到新代码。
- 当前用户口径下，`回购方案 / 回购预案 / stcn 拟回购股份快讯` 都不再算应保留样本；不要再拿 `贵州茅台回购方案` 或 `乖宝宠物拟回购股份` 当正例去保留。
- `纳斯达克综合指数跌逾1% 芯片半导体股票集体下跌` 这类“外盘指数 + 明确题材板块联动 + A股候选映射”的样本不是低信号噪音；应按 `market_reference` 保留在 report，仅从 `audit-suspicious` 异常告警口径排除。
- `PCB概念走强 大族激光等股价创新高` 这类“A股概念走强 + 涨停/创新高/大涨 + 明确题材映射”的样本不是低信号噪音；应按 `market_reference` 保留 report 价值，仅从 `audit-suspicious` 异常告警口径排除。
- `创新药概念活跃 昂利康2连板` 这类“A股概念活跃 + 连板/涨逾/涨近/涨幅居前 + 明确题材映射”的样本不是低信号噪音；应按 `market_reference` 保留 report 价值，仅从 `audit-suspicious` 异常告警口径排除。
- `半导体板块震荡走强 天岳先进涨近20%` 这类“A股板块震荡走强 + 涨近/涨逾/涨幅居前 + 明确题材映射”的样本不是低信号噪音；应按 `market_reference` 保留 report 价值，仅从 `audit-suspicious` 异常告警口径排除。
- `港股AI应用股拉升 智谱涨逾15%` 这类“港股主题股异动 + 明确A股题材映射”的样本不是低信号噪音；应按 `market_reference` 保留 report 价值，仅从 `audit-suspicious` 异常告警口径排除。
- `*ST海源：关于新增诉讼的公告`、`海南海药：关于公司提起诉讼的公告` 这类 summary 只有标题的短标题诉讼材料公告，当前按 audit 尾噪处理；不要扩大成“所有诉讼公告都是噪声”，有金额、判决、冻结、败诉、赔偿、重大进展的诉讼风险仍要保留。
- `联美量子股份有限公司关于子公司募集资金账户被冻结的公告` 已按用户判断归入 audit 尾噪；后续同族 `募集资金账户被冻结` 不要反向当强风险正例，也不要因此扩成所有冻结公告都降噪。
- `social-sidecar` 当前只有 `fixture` 路径完成了真实写盘与展示验收；不要把这一步误写成“weibo 已可用”或“社交源已接入主评分”。
- `三大指数全部翻红` 这类 `cls market_move` 弱综述，真正漏回 report 的原因不一定在标题，而可能是正文里带了 `跌超1%` 这种通用 `market_move` 宽词；优先按“指数综述变体”在 `text_report` 收口。
- `stcn` 转载互动平台口径时，`持续加大业务投入 + 在手订单充裕 + 交付有序进行` 这类经营表态，即使带主题也更像弱经营更新；优先按 `text_report` 的窄 reply 规则过滤，不要直接当真订单催化保留。
- `cls` 当前主线允许保留一部分全球资讯和大宗商品异动；像 `WTI原油期货跌破86美元/桶` 这类样本，不要因为不是 A 股单票新闻就默认压掉。
- `第二届世界人形机器人运动会将于8月在京举办` 这类带明确时间点和主题承接的事件稿，现阶段更像 legit 保留样本，不要直接按“新闻发布会/活动”会务噪音处理。
- 分层骨架和层内排序都已经补过后，下一轮优先只读看 live 头部；不要在 `audit-suspicious=0` 的状态下继续为了更干净头部过拟合。
- `miit:fetch_error` 是采集源问题；不要混成 `text_report` 规则失败，需单独按 collector/source 排障。
- 推送前不要假定远端仓库已存在；先 `gh repo view owner/repo`，不存在再 `gh repo create owner/repo ...`。

## Scope
- 当前主线是 `live` 样本收口和第一批官方源扩展，不要顺手扩宽题材库。
- 社交/自媒体即使后续接入，也应先作为线索层，不能直接进主信号层。
- `social-sidecar` 当前边界是“写 sidecar + 展示 sidecar”，不是“改主评分权重”。
- `冻结 / 解除冻结` 这类标题不能自动按低信号材料处理；先判断是程序性披露，还是实质风险状态变化。
- `申请破产清算` 不能继续留在 `corporate_disclosure / neutral`；这类样本应优先回正到 `reorganization_risk / bearish`。
# 避坑记录 - 2026-05-15
- `新能源汽车安全管理工作视频会`：这是公共事务/会议类快讯；在没有明确政策强约束、订单、上市公司可交易链条时，按 audit 尾噪处理，不扩新能源车题材库。
- `全球首个海底数据中心落户东海`：这是项目示范/科普型快讯；只用最窄条件 `海底数据中心 + 落户 + 东海` 降噪，不要泛化到所有数据中心、算力订单或上市公司业务进展。
- `拜访 + 交流 + 持续推进交流对接/进行了交流`：这是弱合作线索尾噪；出现 `签署/中标/订单/合同/采购` 时不能套用。
- 本机如出现 `git status` / `git diff` / 文件头部读取卡住，先查并清理卡住的只读进程；不要在未确认 Git 可用时宣称已推送。
