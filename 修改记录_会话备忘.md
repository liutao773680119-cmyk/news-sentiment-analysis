# 修改记录

用于记录“某一轮会话具体做了什么、停在什么地方”。  
这里按会话顺序追加；长期结论仍以 `progress.md / findings.md` 为准。

## Session Record

### 2026-05-21（A股指数+板块活跃误报收口与后台 clean 复核）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `A股指数+板块活跃 market_reference 收口与后台 clean 复核`
- 触发背景：
  - 后台连续出现：
    - `2026-05-21_10:15:11` -> `watchdog_status=alert`、`suspicious_count=1`
    - `2026-05-21_10:26:15` -> `watchdog_status=alert`、`suspicious_count=1`
    - `2026-05-21_10:37:04` -> `watchdog_status=alert`、`suspicious_count=1`
  - 即时审计命中：
    - `创业板指、深证成指均涨逾2% 半导体、券商等板块活跃`
    - `reason=general_fast_news_with_theme`
- 本轮判断：
  - 这条不是 source 问题，也不是真风险。
  - 本质是 `A股指数 + 板块活跃` 的新 `market_reference` 模板，和之前 `纳指跌+半导体`、`PCB概念走强`、`创新药概念活跃`、`半导体板块震荡走强` 同族。
- 本轮修改：
  - `tests/test_audit_suspicious.py`
    - 新增红灯回归：
      - `test_audit_suspicious_keeps_a_share_index_sector_active_as_market_reference`
  - `src/news_sentiment/cli.py`
    - 新增最窄 helper：
      - `stcn + general_fast_news + A股指数关键词 + 板块活跃 + 涨幅热度词`
    - 保持只退出 audit 异常，不改 report 和 score。
- 验证：
  - 红灯：
    - `./.venv/bin/pytest tests/test_audit_suspicious.py -k 'a_share_index_sector_active_as_market_reference' -q`
    - 先失败：`suspicious_count=1`
  - 绿灯：
    - `./.venv/bin/pytest tests/test_audit_suspicious.py -k 'a_share_index_sector_active_as_market_reference or global_index_sector_move_as_market_reference or a_share_concept_move_as_market_reference or a_share_concept_active_limit_up_as_market_reference or a_share_sector_strengthening_as_market_reference' -q`
    - `5 passed`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20`
    - `suspicious_count=0`
  - 后台自然循环：
    - `2026-05-21_10:47:54` -> `watchdog_status=clean`、`failed_sources=none`、`suspicious_count=0`
- 停点：
  - 当前内容侧已收口。
  - 后续若再出指数类样本，先看是否仍具备题材和热度上下文；不要把所有指数综述一起放过。

### 2026-05-20（交易所问询材料降噪与后台 recovered 复核）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `交易所问询材料 audit 降噪与后台 recovered 复核`
- 本轮目标:
  - 清掉当前两条交易所问询材料造成的后台内容异动。
  - 做标准交接并推送。
- 已完成:
  - 为 `中国高科...股价波动事项的问询函` 和 `百通能源...审核问询函有关财务事项的说明` 补红灯测试和最窄降噪规则。
  - `audit-suspicious` 与 `text_report` 同步补词，避免后台与报告口径分裂。
  - 保留 `佳通轮胎披露收到中国证监会立案告知书` 作为反例，确认真风险仍保留。
  - 提交并推送：
    - `a585e70 fix: suppress exchange inquiry material noise`
  - 复核后台自然循环，确认：
    - `22:22:56` -> `alert` 但 `suspicious_count=0`
    - `22:36:54` -> `recovered`
    - 最新状态 -> `clean`
- 未完成:
  - 仍需只读观察 `eia_wpsr` 是否后续再次间歇性失败。
- 关键命令/结果:
  - `./.venv/bin/pytest tests/test_audit_suspicious.py::test_audit_suspicious_skips_current_exchange_inquiry_material_noise -q` -> 先失败 `suspicious_count=3`，修后 `1 passed`
  - `./.venv/bin/pytest tests/test_audit_suspicious.py -q` -> `64 passed`
  - `./.venv/bin/pytest tests/test_audit_suspicious.py tests/test_text_report_sorting.py::test_write_text_report_filters_financing_inquiry_reply_and_judicial_unfreeze_without_hiding_catalyst -q` -> `65 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20` -> `suspicious_count=0`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment watchdog-once --source all --limit 20` -> `failed_sources=eia_wpsr:fetch_error`、`suspicious_count=0`
  - `rg -n '^=====|watchdog_status=|failed_sources=|suspicious_count=' /tmp/news-sentiment-watch.log | tail -n 40` -> 已看到 `22:36:54 recovered`，之后 `clean`
- 下一步:
  - 继续只读观察后台下一轮：
    - `rg -n '^=====|watchdog_status=|failed_sources=|suspicious_count=' /tmp/news-sentiment-watch.log | tail -n 40`
  - 再跑当前即时审计：
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20`
- 新增/更新的避坑记录:
  - 不要把所有 `问询函` 一刀切降噪。
  - `股价波动事项的问询函` 和 `审核问询函有关财务事项的说明` 只按材料文案最窄口径处理。

## Session Record

### 2026-05-20（6小时汇总与股份冻结异动收口）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `6小时 watchdog 汇总与股份冻结 audit 异动收口`
- 本轮目标:
  - 让后台每 6 小时生成汇总报告。
  - 处理当前唯一 `audit-suspicious` 异动。
  - 提交推送并重启 watchdog。
- 已完成:
  - 新增 `watchdog-summary --hours 6`。
  - 后台 loop 新增 6 小时 summary 调度。
  - 新增 summary 单测与 loop 调度测试。
  - 当前异动 `中农发种业集团股份有限公司关于股东所持部分股份冻结的公告` 已按最窄 audit 尾噪规则收口。
  - 新增股份冻结回归测试。
- 关键命令/结果:
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment watchdog-summary --hours 6` -> 已生成 summary。
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20` -> `suspicious_count=0`
  - `tests/test_watchdog_summary.py tests/test_watchdog_loop_script.py tests/test_watchdog.py -q` -> `6 passed`
  - 股份冻结定向测试 -> `1 passed`
- 下一步:
  - 提交推送后重启 watchdog。
  - 重启后确认新 PID 和 heartbeat。
- 新增/更新的避坑记录:
  - 6 小时汇总不自动改规则。
  - `股东所持部分股份冻结` 只按最窄 audit 尾噪处理，不扩成所有冻结公告。

## Session Record

### 2026-05-19（夜间 watchdog alert：年报问询函、会见交流、贵金属播报）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `5/19 夜间 watchdog alert 收口：年报问询函材料、会见交流与贵金属播报`
- 本轮目标:
  - 处理 20:34-21:44 新滚入的后台 `audit-suspicious` 异动，并做标准交接。
- 已完成:
  - 固定并处理 5 条年报问询函材料/回复变体。
  - 固定并处理 2 条会见交流类低信号快讯：
    - 外汇局会见友邦保险主席
    - 江苏省委书记会见 AMD 董事会主席兼 CEO
  - 固定并处理 1 条现货黄金日内跌幅播报。
  - 更新标准交接文件：
    - `progress.md`
    - `task_plan.md`
    - `findings.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - 本轮未 commit/push。
  - 后台自动日志最后仍停在旧 `21:44 alert=2`，需等下一轮 watchdog 自动跑完后确认 clean。
- 关键命令/结果:
  - `./.venv/bin/pytest tests/test_audit_suspicious.py -k 'current_low_signal_batch_20260519_night or current_low_signal_batch_20260518 or precious_metal_spot_move or chairman_meeting' -q` -> `4 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20` -> `suspicious_count=0`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=568 normalized_news=568 events=457 analyses=457 failed_sources=none`
  - 刷新后 `audit-suspicious --limit 20` -> `suspicious_count=0`
  - `./.venv/bin/python -m py_compile src/news_sentiment/cli.py tests/test_audit_suspicious.py` -> passed
  - `git diff --check` -> passed
- 下一步:
  - 只读观察下一轮后台：
    - `rg -n '^=====|watchdog_status=|failed_sources=|suspicious_count=' /tmp/news-sentiment-watch.log | tail -n 40`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20`
  - 如需推送，再做 scoped commit/push。
- 新增/更新的避坑记录:
  - 会见交流类必须继续排除签署/中标/订单/合同/采购。
  - 现货黄金/白银点位和涨跌幅播报可降噪，但不能扩到黄金产业链新闻。

## Session Record

### 2026-05-19（后台 suspicious 二次降噪：私营融资与 Takeda 海外诉讼）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `后台 suspicious 二次降噪：私营生物医药融资与 Takeda 海外药企诉讼`
- 本轮目标:
  - 处理 5/19 后台新滚入的两条 `general_fast_news_with_theme` 可疑样本，并标准交接推送。
- 已完成:
  - 对 `爱科诺生物医药宣布完成5000万美元C轮融资` 新增红灯测试和最窄降噪规则。
  - 对 `Japan’s Takeda engaged in antitrust scheme to delay generic constipation drug, US jury finds` 新增红灯测试和最窄降噪规则。
  - 手动刷新全源 live，并确认当前 `audit-suspicious=0`。
  - 复核后台日志，确认 11:47 到 13:58 连续多轮 `watchdog_status=clean`。
  - 更新标准交接文件：
    - `progress.md`
    - `task_plan.md`
    - `findings.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - 推送后仍需继续只读观察下一轮后台。
- 关键命令/结果:
  - `./.venv/bin/pytest tests/test_audit_suspicious.py -k 'overseas_pharma_antitrust_lawsuit_theme_spillover or private_biotech_c_round_financing_story or private_robot_financing_general_fast_news' -q` -> `3 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=614 normalized_news=614 events=463 analyses=463 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20` -> `suspicious_count=0`
  - `./.venv/bin/python -m py_compile src/news_sentiment/cli.py tests/test_audit_suspicious.py` -> passed
  - `git diff --check` -> passed
- 下一步:
  - 推送后只读观察后台：
    - `rg -n '^=====|watchdog_status=|failed_sources=|suspicious_count=' /tmp/news-sentiment-watch.log | tail -n 40`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20`
- 新增/更新的避坑记录:
  - 私营融资泛稿不要扩到所有融资新闻。
  - Takeda 海外药企反垄断/诉讼串味不要扩到所有医药新闻。

## Session Record

### 2026-05-19（5/15-5/19 后台 suspicious 回溯与尾噪收口）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `5/15-5/19 后台 suspicious 回溯与尾噪收口`
- 本轮目标:
  - 回查 5/15 到 5/19 的后台异常，确认是否处理完成，并标准交接推送。
- 已完成:
  - 聚合查看 5/15-5/19 watchdog 日志：
    - 5/15-5/18 存在多轮 alert
    - 5/19 当前内容可疑为 0
    - 部分 alert 来自 `szse/csrc/sse_einteractive` 等采集失败
  - 收口内容侧尾噪：
    - 年报问询函审计专项说明 / 法律意见书材料
    - 仲裁进展材料
    - 会见/交流类快讯
    - 现货白银点位播报
    - `irm_cninfo` 纯提问法律投诉
  - 将 `【淘金互动易】` 算力产业链内容按 `market_reference` 处理，保留报告参考价值，只退出后台异常。
  - 更新标准交接文件：
    - `progress.md`
    - `task_plan.md`
    - `findings.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - 推送后还需观察下一轮后台是否继续 `suspicious_count=0`。
- 关键命令/结果:
  - `./.venv/bin/pytest tests/test_audit_suspicious.py -k 'current_low_signal_batch_20260518 or bank_insurance_chairman_meeting_story or annual_inquiry_audit_and_legal_opinion_materials' -q` -> `3 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20` -> `suspicious_count=0`
  - `GIT_OPTIONAL_LOCKS=0 git status --short -uno` -> 8 个文件待提交
- 下一步:
  - 推送后只读观察后台：
    - `rg -n '^=====|watchdog_status=|failed_sources=|suspicious_count=' /tmp/news-sentiment-watch.log | tail -n 40`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20`
- 新增/更新的避坑记录:
  - `suspicious_count=0` 但 `watchdog_status=alert` 时，先看 `failed_sources`，不要误当内容异常继续补规则。

## Session Record

### 2026-05-14（半导体板块震荡走强 market_reference 收口）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `后台 suspicious 口径分层：A股板块异动参考信号保留`
- 本轮目标:
  - 复核新异常 `半导体板块震荡走强 天岳先进涨近20%`
  - 保留题材与报告价值，只从后台异常口径退出
  - 做标准交接并推送
- 已完成:
  - 确认样本已在 `latest_report.txt` 保留，题材为 `半导体`，强度 `79.0`。
  - 新增评分回归 `test_score_event_keeps_a_share_sector_strengthening_as_theme_reference`，确认继续触发 `半导体`。
  - 新增 audit 回归 `test_audit_suspicious_keeps_a_share_sector_strengthening_as_market_reference`，先确认红灯为 `suspicious_count=1`。
  - 扩窄 A 股 market_reference 规则：
    - 标题允许 `概念走强`、`概念活跃` 或 `板块震荡走强`
    - 上下文要求 `连板 / 涨逾 / 涨近 / 涨幅居前 / 涨停 / 创新高 / 大涨`
  - 更新标准交接文件：
    - `progress.md`
    - `task_plan.md`
    - `findings.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - 后台自动 loop 需等下一轮吃到最新提交后再确认是否回 `clean`。
- 关键命令/结果:
  - `./.venv/bin/pytest tests/test_analysis_scoring.py -k 'a_share_sector_strengthening_as_theme_reference or a_share_concept_active_limit_up_as_theme_reference or a_share_concept_move_as_theme_reference or global_index_sector_move_as_theme_reference' -q` -> `4 passed`
  - `./.venv/bin/pytest tests/test_audit_suspicious.py -k 'a_share_sector_strengthening_as_market_reference or a_share_concept_active_limit_up_as_market_reference or a_share_concept_move_as_market_reference or global_index_sector_move_as_market_reference' -q` -> `4 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
  - `git diff --check` -> 通过
- 下一步:
  - 推送后继续只读观察：
    - `rg -n '^=====|watchdog_status=|suspicious_count=|failed_sources=' /tmp/news-sentiment-watch.log | tail -n 20`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
- 新增/更新的避坑记录:
  - A 股板块震荡走强且带涨近/涨逾/涨幅居前上下文时，不要当噪音删；应按 `market_reference` 保留报告，只从后台异常告警排除。

### 2026-05-14（创新药概念活跃 market_reference 收口）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `后台 suspicious 口径分层：A股概念活跃参考信号保留`
- 本轮目标:
  - 复核新异常 `创新药概念活跃 昂利康2连板`
  - 保留题材与报告价值，只从后台异常口径退出
  - 做标准交接并推送
- 已完成:
  - 确认样本已在 `latest_report.txt` 保留，题材为 `创新药`，强度 `79.0`。
  - 新增评分回归 `test_score_event_keeps_a_share_concept_active_limit_up_as_theme_reference`，确认继续触发 `创新药`。
  - 新增 audit 回归 `test_audit_suspicious_keeps_a_share_concept_active_limit_up_as_market_reference`，先确认红灯为 `suspicious_count=1`。
  - 扩窄 A 股概念异动 market_reference 规则：
    - 标题允许 `概念走强` 或 `概念活跃`
    - 上下文要求 `连板 / 涨逾 / 涨近 / 涨幅居前 / 涨停 / 创新高 / 大涨`
  - 更新标准交接文件：
    - `progress.md`
    - `task_plan.md`
    - `findings.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - 后台自动 loop 需等下一轮吃到最新提交后再确认是否回 `clean`。
- 关键命令/结果:
  - `./.venv/bin/pytest tests/test_analysis_scoring.py -k 'a_share_concept_active_limit_up_as_theme_reference or a_share_concept_move_as_theme_reference or hk_ai_application_move_as_theme_reference or global_index_sector_move_as_theme_reference' -q` -> `4 passed`
  - `./.venv/bin/pytest tests/test_audit_suspicious.py -k 'a_share_concept_active_limit_up_as_market_reference or a_share_concept_move_as_market_reference or hk_ai_application_move_as_market_reference or global_index_sector_move_as_market_reference' -q` -> `4 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
  - `git diff --check` -> 通过
- 下一步:
  - 推送后继续只读观察：
    - `rg -n '^=====|watchdog_status=|suspicious_count=|failed_sources=' /tmp/news-sentiment-watch.log | tail -n 20`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
- 新增/更新的避坑记录:
  - A 股概念活跃且带连板/涨逾/涨近/涨幅居前上下文时，不要当噪音删；应按 `market_reference` 保留报告，只从后台异常告警排除。

### 2026-05-14（募集资金账户冻结 audit 尾噪收口）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `后台 suspicious 口径分层：募集资金账户冻结尾噪`
- 本轮目标:
  - 复核夜间持续异常 `联美量子股份有限公司关于子公司募集资金账户被冻结的公告`
  - 按用户判断将其作为噪声降噪
  - 做标准交接并推送
- 已完成:
  - 确认后台线程正常，采集源不是主问题，异常来自 `hard_event_risk_keyword`。
  - 新增红灯测试 `test_audit_suspicious_skips_fundraising_account_freeze_material_notice`，先确认当前会失败为 `suspicious_count=1`。
  - 只补最窄 audit 关键词：
    - `募集资金账户被冻结`
  - 不改 `score_event`，不改 `text_report` 过滤。
  - 更新标准交接文件：
    - `progress.md`
    - `task_plan.md`
    - `findings.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - 后台自动 loop 需等下一轮吃到最新提交后再确认是否回 `clean`。
- 关键命令/结果:
  - `./.venv/bin/pytest tests/test_audit_suspicious.py -k 'fundraising_account_freeze_material_notice' -q` -> 先失败，复现 `suspicious_count=1`
  - `./.venv/bin/pytest tests/test_audit_suspicious.py -k 'fundraising_account_freeze_material_notice or waiting_freeze_notice or short_litigation_material_notices or major_litigation_disclosure' -q` -> `4 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
  - `git diff --check` -> 通过
- 下一步:
  - 推送后继续只读观察：
    - `rg -n '^=====|watchdog_status=|suspicious_count=|failed_sources=' /tmp/news-sentiment-watch.log | tail -n 20`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
- 新增/更新的避坑记录:
  - `募集资金账户被冻结` 已按用户判断归入 audit 尾噪；后续不要反向当强风险正例。

### 2026-05-13（续：短标题诉讼材料 audit 尾噪收口）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `后台 suspicious 口径分层：短标题诉讼材料尾噪`
- 本轮目标:
  - 复核后台新异常中的两条诉讼公告
  - 判断是否属于噪声
  - 按最窄口径退出 `audit-suspicious`
  - 做标准交接并提交推送
- 已完成:
  - 确认后台线程正常，异常来自 `hard_event_risk_keyword`。
  - 确认两条样本 summary 只有标题，且不在 `latest_report.txt` 头部：
    - `*ST海源：关于新增诉讼的公告`
    - `海南海药：关于公司提起诉讼的公告`
  - 新增红灯测试 `test_audit_suspicious_skips_exchange_short_litigation_material_notices`，先确认当前会失败为 `suspicious_count=2`。
  - 只补两个最窄关键词：
    - `新增诉讼的公告`
    - `提起诉讼的公告`
  - 更新标准交接文件：
    - `progress.md`
    - `task_plan.md`
    - `findings.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - 后台自动 loop 需等下一轮吃到最新提交后再确认是否回 `clean`。
- 关键命令/结果:
  - `./.venv/bin/pytest tests/test_audit_suspicious.py -k 'short_litigation_material_notices' -q` -> 先失败，复现 `suspicious_count=2`
  - `./.venv/bin/pytest tests/test_audit_suspicious.py -k 'short_litigation_material_notices or major_litigation_and_filing_progress_notices or cumulative_new_litigation_arbitration_disclosure or major_litigation_disclosure' -q` -> `4 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
  - `git diff --check` -> 通过
- 下一步:
  - 推送后继续只读观察：
    - `rg -n '^=====|watchdog_status=|suspicious_count=|failed_sources=' /tmp/news-sentiment-watch.log | tail -n 20`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
- 新增/更新的避坑记录:
  - 短标题诉讼材料公告只按 audit 尾噪处理；不要误伤有金额、判决、冻结、败诉、赔偿、重大进展的真实诉讼风险。

### 2026-05-13（续：港股 AI 应用异动 market_reference 收口）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `后台 suspicious 口径分层：港股主题异动参考信号保留`
- 本轮目标:
  - 复核后台新异常 `港股AI应用股拉升 智谱涨逾15%`
  - 判断是否应降噪
  - 按用户确认保留参考价值，只从后台异常口径退出
  - 做标准交接并推送
- 已完成:
  - 确认后台线程正常，`failed_sources=none`，异常来自 `audit-suspicious` 的 `general_fast_news_with_theme` 命中。
  - 判断该样本属于港股主题股异动参考信号：
    - `港股AI应用股拉升`
    - 智谱涨逾 15%
    - 命中 `AI应用` 题材
  - 新增港股主题异动 `market_reference` 规则：
    - 保留 `score_event` 侧 `AI应用` 题材映射
    - 保留 `impact_score=79.0`
    - 仅在 `audit-suspicious` 里跳过告警
  - 新增回归：
    - `test_score_event_keeps_hk_ai_application_move_as_theme_reference`
    - `test_audit_suspicious_keeps_hk_ai_application_move_as_market_reference`
  - 更新标准交接文件：
    - `progress.md`
    - `task_plan.md`
    - `findings.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - 后台自动 loop 最新日志可能仍停在旧 `alert`；推送后等下一轮自动跑完再只读复核。
- 关键命令/结果:
  - `./.venv/bin/pytest tests/test_analysis_scoring.py -k 'hk_ai_application_move_as_theme_reference or a_share_concept_move_as_theme_reference or global_index_sector_move_as_theme_reference or general_fast_news_with_theme' -q` -> `4 passed`
  - `./.venv/bin/pytest tests/test_audit_suspicious.py -k 'hk_ai_application_move_as_market_reference or a_share_concept_move_as_market_reference or global_index_sector_move_as_market_reference or night_session_commodity_opening_story' -q` -> `4 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
  - `git diff --check` -> 通过
- 下一步:
  - 推送后继续只读观察：
    - `rg -n '^=====|watchdog_status=|suspicious_count=|failed_sources=' /tmp/news-sentiment-watch.log | tail -n 20`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
- 新增/更新的避坑记录:
  - 港股主题股异动且有明确 A 股题材映射时，不要当噪音删；应按 `market_reference` 保留报告，只从后台异常告警排除。

### 2026-05-13（续：A股概念异动 market_reference 收口）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `后台 suspicious 口径分层：A股概念异动参考信号保留`
- 本轮目标:
  - 复核后台新异常 `PCB概念走强 大族激光等股价创新高`
  - 判断它是否应被降噪
  - 按用户确认保留参考价值，只从后台异常口径退出
  - 做标准交接并推送
- 已完成:
  - 确认该样本不是进程异常，而是 `audit-suspicious` 的 `general_fast_news_with_theme` 口径命中。
  - 判断该样本属于 A 股题材异动参考信号：
    - `PCB概念走强`
    - 大族激光、生益科技涨停
    - 股价创新高
  - 新增 A 股概念异动 `market_reference` 规则：
    - 保留 `score_event` 侧 `PCB` 题材映射
    - 保留 `impact_score=79.0`
    - 仅在 `audit-suspicious` 里跳过告警
  - 新增回归：
    - `test_score_event_keeps_a_share_concept_move_as_theme_reference`
    - `test_audit_suspicious_keeps_a_share_concept_move_as_market_reference`
  - 更新标准交接文件：
    - `progress.md`
    - `task_plan.md`
    - `findings.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - 当前 live 文件已滚动，不再含该标题；已用固定回归锁定口径。
- 关键命令/结果:
  - `./.venv/bin/pytest tests/test_analysis_scoring.py -k 'a_share_concept_move_as_theme_reference or global_index_sector_move_as_theme_reference or general_fast_news_with_theme' -q` -> `3 passed`
  - `./.venv/bin/pytest tests/test_audit_suspicious.py -k 'a_share_concept_move_as_market_reference or global_index_sector_move_as_market_reference or night_session_commodity_opening_story' -q` -> `3 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
  - 后台最近多轮已回到 `watchdog_status=clean`、`suspicious_count=0`
  - `git diff --check` -> 通过
- 下一步:
  - 推送后继续只读观察：
    - `rg -n '^=====|watchdog_status=|suspicious_count=|failed_sources=' /tmp/news-sentiment-watch.log | tail -n 20`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
- 新增/更新的避坑记录:
  - A 股概念走强且带涨停/创新高/大涨上下文时，不要当噪音删；应按 `market_reference` 保留报告，只从后台异常告警排除。

### 2026-05-13（后台 suspicious 口径分层：低信号降噪与外盘题材参考保留）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `后台 suspicious 口径分层：低信号降噪与外盘题材参考保留`
- 本轮目标:
  - 查看后台连续 alert 是否是进程异常还是内容口径异常
  - 处理可确认的低信号样本
  - 将有参考价值的外盘题材联动从“异常”改为“市场参考信号”
  - 做标准交接并推送
- 已完成:
  - 确认 `news-sentiment-watch` 后台进程正常，`heartbeat` 正常，主要问题是 `audit-suspicious` 内容口径。
  - 补齐低信号材料/播报类 audit 豁免：
    - `股票交易异常波动问询函` 回函
    - `年报问询函的专项说明`
    - `国内商品期货夜盘开盘 + 涨近/跌近/涨超/跌超`
  - 根据用户判断撤回对 `纳斯达克综合指数跌逾1% 芯片半导体股票集体下跌` 的低信号降噪。
  - 新增 `market_reference` 口径：
    - 保留该条在 `latest_report.txt`
    - 保留 `半导体` 题材和 A 股候选映射
    - 仅在 `audit-suspicious` 异常巡检里跳过，不再让后台持续 alert
  - 更新标准交接文件：
    - `progress.md`
    - `task_plan.md`
    - `findings.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - 尚未等待下一轮后台 loop 自动吃到最新代码；推送后下一轮只读确认即可。
- 关键命令/结果:
  - `./.venv/bin/pytest tests/test_analysis_scoring.py -k 'global_index_sector_move_as_theme_reference or general_fast_news_with_theme' -q` -> `2 passed`
  - `./.venv/bin/pytest tests/test_audit_suspicious.py -k 'global_index_sector_move_as_market_reference or night_session_commodity_opening_story' -q` -> `2 passed`
  - 当前数据直接复算 `collect_suspicious_candidates(...)` -> `current_suspicious_count 0`
  - `rg -n -C 4 '纳斯达克综合指数跌逾1% 芯片半导体股票集体下跌' data/reports/latest_report.txt` -> 报告仍保留该条
  - `git diff --check` -> 通过
- 下一步:
  - 先看下一轮后台：
    - `rg -n '^=====|watchdog_status=|suspicious_count=|failed_sources=' /tmp/news-sentiment-watch.log | tail -n 20`
  - 再跑：
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 如果后台仍报同一条，先确认 loop 是否已经吃到最新提交，不要继续加 report 过滤。
- 新增/更新的避坑记录:
  - 外盘指数 + 明确题材板块联动 + A股候选映射的样本按 `market_reference` 保留 report，只从 audit 异常口径排除。

### 2026-05-11（cninfo 年报问询函与新增诉讼材料降噪）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `cninfo 年报问询函与新增诉讼材料降噪`
- 本轮目标:
  - 收口 `cninfo` 年报问询函回复/发行优先股相关高分材料噪音
  - 不扩题材库，不改 `event_merge / analysis / source enable`
  - 在 `audit-suspicious` 与 `live-smoke` 双链路同步验收
- 已完成:
  - 在 `cli.py` 规则口径补齐 8 条边界样本关键字：
    - 年报问询函相关 `年报有关事项问询函 / 年报问询函之回复 / 年报问询函》回复 / 监管问询函的回复 / 问询函相关问题之专项核查意见`
    - `向特定对象发行优先股 / 发行优先股`
    - `新增诉讼及进展情况`
    - 再补 `持股5%以上股东股份解除冻结 / 控股股东所持公司部分股份解除冻结 / 诉讼案件进展 / 诉讼案件进展情况`
  - 新增 `tests/test_audit_suspicious.py` 3 个回归：
    - `test_audit_suspicious_skips_cninfo_inquiry_reply_related_batch_8_titles`
    - `test_audit_suspicious_skips_cninfo_annual_inquiry_reply_with_quote_style`
    - `test_audit_suspicious_skips_cninfo_added_litigation_progress_update_without_themes`
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_audit_suspicious.py -q` -> `41 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20` -> `suspicious_count=0`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=576 normalized_news=576 events=440 analyses=440 failed_sources=none`
  - `rg -n "年报有关事项问询函|问询函》回复|发行优先股|新增诉讼及进展情况|瑞茂通" data/reports/latest_report.txt` -> 无命中
- 下一步:
  - 先只读查看：
    - `sed -n '1,160p' data/reports/latest_report.txt`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 无异常则可直接收尾提交

### 2026-05-08（sse_einteractive / irm_cninfo live 尾噪补收口 + 收尾待推送）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `sse_einteractive / irm_cninfo live 尾噪补收口 + 收尾待推送`
- 本轮目标:
  - 继续清 `latest_report.txt` 头部残留的 `sse_einteractive / irm_cninfo` 弱问答
  - 不扩题材库，不动 `event_merge / analysis / source enable`
  - 做完后直接进入推送收尾
- 已完成:
  - 在 `sse_einteractive` 收掉：
    - `安通控股：强烈质疑公司管理层不作为...`
    - `安通控股：公司如此二级市场存在财务虚报配合股东压价增持...`
    - `精工钢构：2020年高调提出的“十年千亿产值”...知行合一在哪里`
  - 在 `irm_cninfo` 收掉：
    - `中超控股：网传中标国家管网集团2026年度电缆集约化采购项目8.79个亿是真的吗`
    - `青鸟智控：公司有涉及光伏储能相关业务吗`
  - 保留了 `精工钢构：业务是否涉及数据中心及火箭发射厂...` 这条实质算力问答
  - 已更新交接文件：
    - `progress.md`
    - `task_plan.md`
    - `findings.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - 还没 commit / push
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -q` -> `242 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -q -k 'sse_einteractive_stock_price_complaints_with_generic_market_reply'` -> `1 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -q -k 'irm_cninfo_weak_order_contract_and_market_move_replies_without_hiding_substantive_progress'` -> `1 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -q -k 'irm_cninfo_weak_theme_inquiry_replies_without_hiding_substantive_progress'` -> `1 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=449 normalized_news=449 events=395 analyses=395 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
  - `rg -n '青鸟智控：|中超控股：|安通控股：|精工钢构：' data/reports/latest_report.txt` -> 只剩 `精工钢构` 实质算力问答
- 下一步:
  - `git status --short`
  - `git add src/news_sentiment/reporting/text_report.py tests/test_text_report_sorting.py progress.md task_plan.md findings.md task_registry.md 修改记录_会话备忘.md 避坑记录.md`
  - `git commit`
  - `git push`
- 新增/更新的避坑记录:
  - `irm_cninfo` question-only live 样本先看 `summary` 是否根本没有回复正文
  - `sse_einteractive` 弱抱怨问答优先按 `title + canned reply` 最窄组合收，不先动 subtype

### 2026-05-08（eia_wpsr 修复 + live report/audit 尾噪收口 + 收尾交接）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `eia_wpsr 修复 + live report/audit 尾噪收口 + 收尾交接`
- 本轮目标:
  - 先确认后台 loop 是否正常
  - 单拆 `eia_wpsr:parse_error`
  - 再按 live 样本逐条清 `report / audit` 尾噪
  - 最后做收尾交接
- 已完成:
  - 确认后台 `news-sentiment-watch` 正常滚动，最新 3 个完整块均为 `failed_sources=none`、`suspicious_count=0`
  - 修复 `eia_wpsr` 日期解析，仅放宽到支持有点/无点月份
  - 在 `audit-suspicious` 收掉：
    - `关于全资子公司仲裁事项的进展公告`
    - `现货白银震荡走高，涨近1%`
  - 在 `text_report` 收掉：
    - `新余国科`
    - `华天科技` 华羿微电两条弱问答
    - `协创数据`
    - `东利机械`
    - `保利发展...保利定转2026年付息公告`
  - 已更新交接文件：
    - `progress.md`
    - `task_plan.md`
    - `findings.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - `sse_einteractive` 头部问答边界还没继续拆
  - 没有重开 `HF Sinclair / AIG` 旧全球边界
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_eia_wpsr_collector.py -q` -> `6 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -q` -> `242 passed`
  - `./.venv/bin/python -m pytest tests/test_audit_suspicious.py -q` -> `33 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=459 normalized_news=459 events=402 analyses=402 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
  - `rg -n '^===== |failed_sources=|suspicious_count=' /tmp/news-sentiment-watch.log | tail -n 9` -> 最新 3 个完整块均为 `failed_sources=none`、`suspicious_count=0`
- 下一步:
  - `sed -n '1,180p' data/reports/latest_report.txt`
  - 如 `精工钢构 / 安通控股` 继续稳定占头部，再单拆 `sse_einteractive` 问答家族
- 新增/更新的避坑记录:
  - analyses 真路径是 `data/events/event_analysis.jsonl`
  - live 重跑后不要硬绑旧 `event_id`

### 2026-05-01（live report 尾噪收口 + 后台 loop 巡检 + 标准交接）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `live report 尾噪收口 + 后台 loop 巡检 + 标准交接`
- 本轮目标:
  - 跟着后台 `news-sentiment-watch` 看新样本
  - 继续清掉 `stcn / irm_cninfo` 新回流的头部尾噪
  - 做一次标准交接并同步远端
- 已完成:
  - 核对后台 loop 正常运行，最近自动落盘到 `2026-05-01_20:52:50`
  - 连续收掉：
    - `刘小明在海南商业航天发射场看望慰问...`
    - `福建金森` 纯提问标题弱问答
    - `东方钽业` 估值修复/专项路演/价值宣讲追问
    - `近一周机构调研个股超700只`
    - `安宁股份：目前公司未单独提取钒产品`
  - 已推送代码提交：
    - `881c5d6 fix: trim latest public affairs and irm complaint noise`
    - `772fa56 fix: trim latest survey roundup and platform qa noise`
  - 已更新交接文件：
    - `progress.md`
    - `task_plan.md`
    - `findings.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - 还没等到下一轮自动 loop 吃到 `772fa56`
  - `HF Sinclair` 和 `AIG` 两条全球分区边界还没决定是否要处理
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -q` -> `236 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
  - 串行执行 `write_text_report()` 后读取 `latest_report.txt` -> 本轮目标样本无命中
  - `git push origin HEAD:mvp-foundation` -> 推送成功，远端到 `772fa56`
- 下一步:
  - `tail -n 120 /tmp/news-sentiment-watch.log`
  - `sed -n '1,170p' data/reports/latest_report.txt`
  - 只读观察 `HF Sinclair / AIG` 是否连续多轮仍在头部
- 新增/更新的避坑记录:
  - `report` 重写和读取不要并行
  - 最新 log 块早于最新 commit 时，不要误判新规则没生效

### 2026-04-29（live report 头部弱样本收口 + audit 同步 + 提交）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `live report 头部弱样本收口 + audit 同步 + 提交`
- 本轮目标:
  - 按 2026-04-28 交接先只读复核
  - 处理 2026-04-29 live report 头部新弱样本
  - 同步提交代码修复与标准交接
- 已完成:
  - 只读复核 worktree、计划、进度、发现、README 和 report 头部
  - 确认 `audit-suspicious=0` 时 report 头部仍有弱样本，继续按红灯测试推进
  - 在 `text_report` 收掉：
    - 美股光通信板块开盘普跌
    - 世界银行能源价格预测
    - `【财联社早知道】我国最大规模科学智能集群...` 匿名拼盘稿
    - 中工国际算电协同出海泛问答
    - 股票期权注销/授予价格调整
    - 减持股份触及比例
    - 董事会风险控制委员会工作细则
    - 股权转让交易进展及签署补充协议
    - 累计新增诉讼/仲裁材料
    - 擎天租 Pre-A 融资机器人应用交付能力泛稿
  - 在 `audit-suspicious` 同步豁免：
    - 累计新增诉讼/仲裁材料
    - 私营平台 Pre-A 融资机器人泛稿
  - 新增/扩展回归测试：
    - `tests/test_text_report_sorting.py`
    - `tests/test_audit_suspicious.py`
  - 已提交代码修复：
    - `1fe1a12 fix: filter latest live report noise`
- 未完成:
  - 未 push
  - 未扩题材库
  - 未改 `event_merge / analysis / source enable`
  - 未继续压退市风险/真实并购头部
- 关键命令/结果:
  - `PYTHONPATH=src ./.venv/bin/python -m pytest tests/test_text_report_sorting.py -q` -> `232 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m pytest tests/test_audit_suspicious.py -q` -> `24 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m py_compile src/news_sentiment/cli.py src/news_sentiment/reporting/text_report.py tests/test_audit_suspicious.py tests/test_text_report_sorting.py` -> 通过
  - `git diff --check` -> 通过
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=2003 normalized_news=2003 events=966 analyses=966 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment report` -> 通过
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
  - `latest_report.txt` 目标弱样本搜索 -> 无命中
- 下一步:
  - 先只读查看：
    - `sed -n '1,180p' data/reports/latest_report.txt`
  - 再跑：
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 如果头部仍是退市风险/真实并购，优先停手
  - 如果继续收口，同步补 report 和 audit 红灯测试
- 新增/更新的避坑记录:
  - `report` 和 `audit-suspicious` 必须同步验收；不能只看其中一个
  - `live-smoke` 后样本会刷新，可能把新的 audit 候选带出来；要重跑 audit
  - 头部回到退市风险/真实并购时，不要为了更短继续机械扩词

### 2026-04-28（live-smoke 后 report 头部尾噪收口 + 标准交接）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `live-smoke 后 report 头部尾噪收口 + 标准交接`
- 本轮目标:
  - 接手 `mvp-foundation` worktree，按交接先验收
  - 处理 live report 头部回流的整族低信号样本
  - 同步标准交接，不提交
- 已完成:
  - 只读接手了 `task_plan.md / progress.md / findings.md / README.md / task_registry.md / 避坑记录.md / 修改记录_会话备忘.md`
  - 补齐 `.venv` 缺失依赖 `requests>=2.31`，恢复 `audit-suspicious` CLI 可运行
  - 修复 `text_report`：
    - 有效时间窗只基于已通过市场相关性过滤的事件计算
    - A股主题市场异动回 `A股强催化`，海外单股主题异动继续在 `全球市场与商品`
    - 最窄收掉当前 live 头部的 `irm_cninfo` 弱问答和 `sse/szse` 年报季材料
  - 新增/扩展 `tests/test_text_report_sorting.py` 回归测试，保护：
    - A股指数温度新闻不压过主题异动
    - 海外单股主题异动不误回 A股层
    - 真实并购/中标样本不被同族弱过滤误伤
  - 刷新 `latest_report.txt` 后，本轮目标关键词已全部退出
- 未完成:
  - 没有提交
  - 没有扩题材库
  - 没有改 `event_merge / analysis / source enable`
  - 没有排查 `miit:fetch_error`
- 关键命令/结果:
  - `./.venv/bin/python -m pip install 'requests>=2.31'` -> 已安装 `requests 2.32.5`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=1473 normalized_news=1473 events=760 analyses=760 failed_sources=miit:fetch_error`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -q` -> `231 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment report` -> 通过
  - `rg` 搜索本轮处理关键词 `latest_report.txt` -> 无命中
- 下一步:
  - 先只读查看：
    - `sed -n '1,160p' data/reports/latest_report.txt`
  - 如果头部仍是并购/退市风险/诉讼/合作协议，优先停手或提交
  - 如继续收口，只处理新的整族弱样本，继续走红灯测试 -> 最窄规则 -> report/audit 验证
  - `miit:fetch_error` 另起 collector/source 排障，不和 report 过滤混线
- 新增/更新的避坑记录:
  - `audit-suspicious=0` 后仍要直接看 report 头部
  - report 时间窗要基于有效事件，不要让低信号新材料挤掉旧正例
  - 带题材的海外单股市场异动不能误拉回 A股强催化
  - 年报季材料会被题材映射抬高，应在 report 层按材料口径过滤

### 2026-04-22（social sidecar fixture 最小验收 + 不提交版标准交接）
- Task-ID:
  - `social-sidecar`
- Task-Name:
  - `社交线索层 fixture 最小验收 + 不提交版标准交接`
- 本轮目标:
  - 不改代码，只复核 `social-sidecar` 当前边界并做最小验收
  - 把验收结果同步进交接文件
- 已完成:
  - 只读复核了 `collect-social / SocialSignal / 社交热度观察` 当前实现与测试
  - 跑通：
    - `tests/test_social_collectors.py`
    - `tests/test_report_pipeline.py -k report_reads_social_signals_sidecar_when_present`
    - `collect-social --platform fixture`
  - 已确认：
    - `data/social/social_signals.jsonl` 已真实落盘
    - report 底部存在 `[社交热度观察]`
    - `平台: fixture` 已正常显示
- 未完成:
  - 没有改代码
  - 没有继续做 `weibo` 真抓取生产化
  - 没有提交
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_social_collectors.py -q` -> `3 passed`
  - `./.venv/bin/python -m pytest tests/test_report_pipeline.py -q -k 'report_reads_social_signals_sidecar_when_present'` -> `1 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment collect-social --platform fixture` -> 成功写盘
- 下一步:
  - 若继续 sidecar：
    - 先决定是补文档说明，还是只验证 `weibo` 失败可见性
  - 若继续主线：
    - sidecar 当前可以先停在这个点，不必继续扩

### 2026-04-22（A股强催化 live 小补收口 + 不提交版标准交接）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `A股强催化 live 小补收口 + 不提交版停手交接`
- 本轮目标:
  - 继续沿最新 `A股强催化` 头部做最窄收口
  - 不提交，只把当前停手点写进交接文件
- 已完成:
  - 补掉 3 批 live 弱样本：
    - `irm_cninfo / sse_einteractive` 追问型弱问答
    - `会议资料 / 监管工作函评估回复 / 授信担保 / 框架协议 / 增持计划 / 预留权益失效`
    - `stcn 拟回购股份` 快讯 + 交易所 `回购方案 / 回购预案`
  - 已同步更新：
    - `progress.md`
    - `task_registry.md`
    - `task_plan.md`
    - `findings.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
  - 已确认当前停手点：
    - `A股强催化` 头部只剩 `*ST和科 / 天孚通信 / 永鼎股份`
    - `audit-suspicious --limit 10` 仍为 `0`
- 未完成:
  - 没有提交
  - 没有继续扩源或改主评分
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -q -k 'filters_live_weak_investor_qa_variants_without_hiding_substantive_catalyst or filters_live_exchange_material_variants_without_hiding_substantive_events or filters_investor_qa_and_exchange_material_within_ashare_section'` -> `3 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -q -k 'filters_second_batch_live_investor_qa_variants_without_hiding_substantive_cls_updates or filters_second_batch_live_exchange_material_variants_without_hiding_real_risk'` -> `2 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -q -k 'filters_exchange_buyback_result_purpose_and_plan_materials or filters_weak_acquisition_followup_and_low_signal_increase_plan_and_stcn_buyback_flash'` -> `2 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
  - 手动 `write_text_report()` 后，`latest_report.txt` 不再包含：
    - `乖宝宠物：拟1亿元—2亿元回购股份`
    - `贵州茅台关于以集中竞价交易方式回购股份方案的公告`
- 下一步:
  - 先轻验收，不先改规则：
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
    - `PYTHONPATH=src python3 - <<'PY' ... write_text_report(...) ... PY`
    - `sed -n '1,220p' data/reports/latest_report.txt`
  - 如果头部仍主要是 `*ST和科 / 天孚通信 / 永鼎股份`，优先停手
  - 只有新的整族弱样本重新回流，才继续补 `text_report`

### 2026-04-22（layer ordering + risk subtype 回正）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `A股强催化层内排序微调 + 风险公告 subtype 回正`
- 本轮目标:
  - 不改分层骨架，只微调 `A股强催化` 的层内排序
  - 把当天新冒出的 `ST岭南` 风险公告从 `corporate_disclosure / neutral` 回正
- 已完成:
  - `A股强催化` 层内排序已微调：
    - 真催化/风险事件优先
    - `irm_cninfo / sse_einteractive` 问答后排
    - 一般材料后排
  - `ST岭南` 两条样本已回正为 `legal_dispute / bearish`
  - 已重刷：
    - `merge-events`
    - `analyze-events`
    - `report`
    - `audit-suspicious`
  - 当前 `audit-suspicious --limit 10` 已回到 `0`
- 未完成:
  - 没有继续补新的过滤规则
  - 没有更新交接外的其它文档
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_event_merge.py -q -k 'major_litigation_progress_as_legal_dispute or case_filing_notice_as_legal_dispute'` -> `2 passed`
  - `./.venv/bin/python -m pytest tests/test_analysis_scoring.py -q -k 'major_litigation_progress_as_bearish or case_filing_notice_as_bearish'` -> `2 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -q -k 'demotes_investor_qa_and_exchange_material_within_ashare_section or groups_entries_into_global_multisource_sections or keeps_social_section_after_mainline_sections'` -> `3 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
- 下一步:
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment run-once --source all`
  - 先只读看新的 live 头部；若没有新弱样本，优先停手

### 2026-04-22（global multisource report 分层骨架）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `全球多源主线 report 分层骨架 + sidecar 尾部保持`
- 本轮目标:
  - 把“全球多源主线”从文档目标变成实际报告结构
  - 只改展示层，不动评分和主链路边界
- 已完成:
  - `text_report` 已从单榜混排改成 4 层输出：
    - `A股强催化`
    - `国内政策与监管`
    - `全球政策与监管`
    - `全球市场与商品`
  - `社交热度观察` 仍保留在主报告所有层之后，没有并入主榜
  - 已同步更新：
    - `progress.md`
    - `task_plan.md`
    - `findings.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - 没有开始调各层内部排序
  - 没有新增层摘要/层说明
  - 没有提交
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -q -k 'groups_entries_into_global_multisource_sections or keeps_social_section_after_mainline_sections'` -> `2 passed`
  - `./.venv/bin/python -m pytest tests/test_report_pipeline.py -q` -> `4 passed`
  - `./.venv/bin/python -m pytest tests/test_cli_smoke.py -q` -> `2 passed`
- 下一步:
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment run-once --source all`
  - 先看分层后的层内排序，再决定是否要补最窄过滤

### 2026-04-22（global multisource + social sidecar 标准交接）
- Task-ID:
  - `global-multisource-mainline`
  - `social-sidecar`
- Task-Name:
  - `全球多源主线边界重定标 + 社交 sidecar 最小接线 + 标准交接`
- 本轮目标:
  - 把当前主线从旧 `phase8` 口径切回“全球多源主线”
  - 给社交源补最小 sidecar 闭环，但不改主评分主链路
  - 把当前真实边界、验证结果和下一步同步进标准交接文件
- 已完成:
  - 已把当前主线明确为 `global-multisource-mainline`，不再沿用旧 `phase8-live-boundary` A股单线验收
  - 已补社交 sidecar 最小闭环：
    - `SocialSignal` 数据结构
    - `data/social/social_signals.jsonl`
    - `collect-social --platform fixture|weibo`
    - report 底部 `社交热度观察`
  - 已补 `collect-social` 失败可见性：
    - `weibo` 被访客门/403 挡住时，输出 `warning: social_platform_failed=weibo:fetch_error:...`
    - 退出码仍保持 `0`，不打断主线脚本
  - 已同步更新：
    - `README.md`
    - `progress.md`
    - `task_plan.md`
    - `findings.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - 没有开始做 report 分层骨架
  - 没有把社交热度接进 `analysis/scoring`
  - 没有证明 `weibo` 生产可用
  - 没有提交
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_social_collectors.py -q` -> `3 passed`
  - `./.venv/bin/python -m pytest tests/test_report_pipeline.py -q` -> `4 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -q -k social_signal_section_without_affecting_main_entries` -> `1 passed`
  - `./.venv/bin/python -m pytest tests/test_report_pipeline.py -q -k social` -> `1 passed`
- 下一步:
  - 若继续主线：
    - `sed -n '1,260p' src/news_sentiment/reporting/text_report.py`
    - 先做 report 分层骨架，不要先补更多过滤
  - 若继续社交 sidecar：
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment collect-social --platform fixture`
    - 先验证展示落点，再评估稳定公开入口；不要直接并入主评分
  - 若准备收尾提交：
    - `git status --short`
    - `git add README.md progress.md task_plan.md findings.md task_registry.md 修改记录_会话备忘.md 避坑记录.md src/news_sentiment/cli.py src/news_sentiment/models.py src/news_sentiment/settings.py src/news_sentiment/reporting/text_report.py src/news_sentiment/social_collectors.py tests/test_report_pipeline.py tests/test_text_report_sorting.py tests/test_social_collectors.py docs/plans/2026-04-22-social-signal-sidecar-design.md`
    - `git commit`
- 新增/更新的避坑记录:
  - 社交源当前只做 sidecar，不进主评分
  - `weibo` 当前 visitor gate/403 仍是外部限制，不要误判成主链路 bug
  - 当前更值得优先做的是 report 分层，不是继续沿旧 `phase8` 口径压头部

### 2026-04-22（phase9 hkex staged 标准交接）
- Task-ID:
  - `phase9-source-expansion`
- Task-Name:
  - `hkex staged 英文材料标题最窄过滤续收口 + 标准交接`
- 本轮目标:
  - 把当前 `hkex staged` 的停手点、剩余样本判断和下一步落盘
  - 不再继续改业务规则
  - 让下一个会话可直接决定“提交”还是“继续单源审查”
- 已完成:
  - 已把 `Latest Handoff Snapshot` 更新为当前真实状态
  - 已同步更新：
    - `progress.md`
    - `task_plan.md`
    - `findings.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
  - 已明确当前停手判断：
    - `CONNECTED TRANSACTION - ACQUISITION OF SOFTWARE ASSETS`
    - `CONNECTED TRANSACTION ENTERING INTO THE CAPITAL INCREASE AGREEMENT`
    - `DISCLOSEABLE TRANSACTION: FURTHER INVESTMENT IN PRECIOUS METALS`
    这 3 条当前更像真实交易主标题，先不继续压
- 未完成:
  - 没有提交
  - 没有继续判断这 3 条是否还要下更细规则
- 关键命令/结果:
  - `git status --short` -> 当前仍有业务代码和交接文档未提交
  - `git branch --show-current` -> `mvp-foundation`
- 下一步:
  - 若继续审查：
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment run-once --source hkex`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20`
  - 若准备收尾提交：
    - `git status --short`
    - `git add src/news_sentiment/reporting/text_report.py tests/test_text_report_sorting.py progress.md task_plan.md findings.md task_registry.md 修改记录_会话备忘.md 避坑记录.md`
    - `git commit`
- 新增/更新的避坑记录:
  - `hkex staged` 收到只剩交易主标题时，优先停手交接，不要继续为了“更干净”过拟合
  - 标准交接要把“当前不该再砍的标题”明确写进交接文件，避免下一会话又把它们当噪音下刀

### 2026-04-22（phase9 hkex staged 最窄收口）
- Task-ID:
  - `phase9-source-expansion`
- Task-Name:
  - `hkex staged 英文材料标题最窄过滤续收口`
- 本轮目标:
  - 不开 `hkex` 到 `--source all`
  - 只沿 `run-once --source hkex` 头部连续补最窄回归和过滤
  - 到剩余头部更像交易主标题时及时停手并落盘交接
- 已完成:
  - `tests/test_text_report_sorting.py` 新增一组 `hkex_...` 回归，累计到 `13` 条
  - `text_report.py` 继续补最窄 `hkex` 过滤，收掉：
    - `performance undertaking / results update + convertible bonds accounting treatment / resumption guidance`
    - `AGM/EGM + transaction`
    - `delay in despatch of circular / extension of proposed completion date`
    - `director share acquisition / supplemental announcement / annual caps`
    - `maintenance work contracts / service framework agreement / leasing and licensing framework agreement`
    - `purchase agreements + sales agreements`
    - `results/management accounts + continued suspension of trading`
    - `voluntary announcement acquisition of assets`
    - `connected transaction + continuing connected transaction + lease agreement`
  - 多轮 `run-once --source hkex` 重刷后，当前头部里已主要剩下更像真实交易主标题的样本：
    - `CONNECTED TRANSACTION - ACQUISITION OF SOFTWARE ASSETS`
    - `CONNECTED TRANSACTION ENTERING INTO THE CAPITAL INCREASE AGREEMENT`
    - `DISCLOSEABLE TRANSACTION: FURTHER INVESTMENT IN PRECIOUS METALS`
- 未完成:
  - 没有继续判断这 3 条交易主标题是否还要压
  - 没有把 `hkex` 开进 `--source all`
  - 没有提交
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -q -k 'hkex_'` -> `13 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment run-once --source hkex` -> 已多轮重刷
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20` -> `suspicious_count=0`
- 下一步:
  - 先重跑：
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment run-once --source hkex`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20`
  - 如果头部仍主要是 `software assets / capital increase / precious metals` 这类标题，优先停手，不继续压
  - 只有出现新的明显材料族，才继续补最窄规则
- 新增/更新的避坑记录:
  - `hkex` 这轮不要再去动 `event_merge / analysis`；最有效的刀仍是 `text_report` 词面过滤
  - `CONNECTED / DISCLOSEABLE / MAJOR TRANSACTION` 主干不是默认低信号；要盯材料尾词而不是交易主干
  - `run-once --source hkex` 当前常没有 stdout，验收要直接看 `latest_report.txt`

### 2026-04-21（phase8 live 基线复跑 + 错并修复 + 新漏样收口）
- Task-ID:
  - `phase8-live-boundary`
- Task-Name:
  - `live 样本边界收口 + 交易所同模板错并修复 + irm/equity_incentive 新漏样收口`
- 本轮目标:
  - 先按交接重跑当天 `live-smoke` / `audit-suspicious`
  - 如果没有新弱样本，就停；如果冒出真问题，优先修根因，不继续盲压 report
  - 把本轮修到的错并和新漏样一起补回标准交接
- 已完成:
  - 复跑基线并确认 `audit-suspicious --limit 10` 仍为 `0`
  - 只读定位到 `szse` 同模板退市风险公告错并：`奥特迅 / *ST声迅 / 明德生物 / ST赛为` 被错误合成一个 event
  - `event_merge/core.py` 已修：
    - `hard_event` 先按结构化催化和股票代码判断
    - 股票代码提取补了 `news_id` fallback
    - 四条退市风险公告已从一个错并 event 拆回四条独立 event
  - 继续按新头部收口：
    - `irm_cninfo` 的 question-only 弱问答 `新雷能 / 山东威达` 已按最窄 title-only 规则过滤
    - `szse equity_incentive` 的 `限制性股票激励计划有关事项的核查意见` 已补精确词面过滤
    - `cls` 的单票跌停回应稿 `股价“一”字跌停 英维克最新回应` 已按 `market_move + 无题材 + 回应解释口径` 过滤
    - 当天新冒头的一组 `irm_cninfo` 弱回复也已按最窄 title+reply 规则过滤：
      - `协创数据` 的经营范围介绍 / 订单充裕口径
      - `协鑫集成` 的“无未披露重大资产重组”回复
      - `通化金马` 的“密切关注 + 审慎论证”
      - `宏明电子` 的并购方向泛回复 / 小批量供货但收入占比较小
      - `大华股份` 的减持预披露规则追问
      - `海康威视` 的继续回购诉求
      - `茂化实华` 的定增正常推进回复
    - `cls` 的 `三大指数全部翻红` 已按 A 股指数综述变体过滤
    - `stcn` 的 `协创数据：2026年将持续加大算力业务投入 目前在手订单充裕` 已按互动平台经营口径转发过滤
  - 已重刷：
    - `merge-events`
    - `report`
    - 当前 `latest_report.txt` 里 `新雷能 / 山东威达 / 光大同创 / 英维克最新回应 / 协创数据 / 协鑫集成 / 宏明电子 / 通化金马 / 大华股份 / 海康威视 / 茂化实华` 已退出
- 未完成:
  - 没有继续往下压当前新的 `cls/stcn` 市场异动头部
  - 没有继续推进 `hkex staged`
  - 没有做提交
- 关键命令/结果:
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
  - `./.venv/bin/python -m pytest tests/test_event_merge.py -k "different_szse_delisting_risk_notices" -q` -> `1 passed`
  - `./.venv/bin/python -m pytest tests/test_event_merge.py -k "groups_structured_cninfo_catalyst_documents or classifies_delisting_risk_notice_subtype or classifies_other_risk_warning_notice_subtype" -q` -> `3 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "question_only_titles or committee_verification_notice" -q` -> `2 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "compute_infra_fallback or disclosure_threshold_and_low_revenue_replies or exchange_equity_incentive_audit_materials_without_theme or current_live_equity_incentive_unmet_exercise_condition_notice" -q` -> `3 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "cls_single_stock_limit_down_response_without_theme" -q` -> `1 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "keeps_cls_single_stock_market_move_for_global_news_collection or robot_half_marathon_supply_chain_story_without_hiding_robot_order or question_only_titles or committee_verification_notice" -q` -> `4 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "current_live_irm_weak_replies_without_hiding_substantive_progress" -q` -> `1 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "current_live_irm_question_only_titles_without_hiding_substantive_progress or irm_cninfo_disclosure_threshold_and_low_revenue_replies_without_hiding_substantive_progress or compute_infra_fallback or cls_single_stock_limit_down_response_without_theme" -q` -> `3 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "cls_three_major_indices_roundup_variant or stcn_interactive_order_plenty_update_without_hiding_substantive_order" -q` -> `2 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "filters_single_ashare_index_roundup_fast_news or filters_stcn_operational_update_with_stable_order_wording or keeps_cls_single_stock_market_move_for_global_news_collection or robot_half_marathon_supply_chain_story_without_hiding_robot_order" -q` -> `4 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment merge-events` -> `events.jsonl` 已反映拆分后的四条退市风险公告
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment report` 与直接 `PYTHONPATH=src` 调 `write_text_report()` -> `新雷能 / 山东威达 / 光大同创 / 英维克最新回应 / 协创数据 / 协鑫集成 / 宏明电子 / 通化金马 / 大华股份 / 海康威视 / 茂化实华 / 三大指数全部翻红 / 协创数据：2026年将持续加大算力业务投入 目前在手订单充裕` 不再出现在 `latest_report.txt`
- 下一步:
  - 先重跑：
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 只读判断后，当前剩余头部里的 `乌克兰机器人扩单 / WTI跌破86 / 世界人形机器人运动会` 更像 legit 保留样本
  - 如果下一轮头部仍主要是这类样本，优先停手，不继续过拟合
- 新增/更新的避坑记录:
  - `hard_event` 同模板交易所公告不能只靠标题相似度归并，必须先看股票代码
  - 修完 `event_merge` 后如果不重跑 `merge-events`，`events.jsonl` 会继续保留旧错并结果
  - `irm_cninfo` 当前 live 有 question-only 样本，不能继续假设一定有回复正文
  - `cls` 的单票跌停回应样本不要因为 `跌停` 就保留；如果是 `无题材 + 回应解释口径`，优先在 `text_report` 窄口径过滤
  - `report` 命令在当前环境里有时看起来执行成功，但 `latest_report.txt` 不一定按最新源码重算；要直接核对目标样本是否退出，必要时用 `PYTHONPATH=src` 调 `write_text_report()` 强制刷新
  - `三大指数全部翻红` 这类标题本身看着像弱综述，但真正把它抬进 report 的常见原因是正文里混入 `跌超1%` 之类 `market_move` 宽词；优先在 `text_report` 按“指数综述变体”过滤，不要误判成主题识别问题
  - `cls` 当前 global news 口径下，`WTI原油期货` 异动和部分海外主题快讯是有意保留的；不要因为它们不是 A 股单票催化就顺手清掉

### 2026-04-21（phase8 live 边界续收口）
- Task-ID:
  - `phase8-live-boundary`
- Task-Name:
  - `live 样本边界收口 + irm 问答/stcn 栏目稿/否定式回应降噪`
- 本轮目标:
  - 继续按当天 `live-smoke` 头部收掉 `stcn` 券商评论/摘要拼盘和 `irm_cninfo` 新弱问答
  - 只做最窄过滤和必要的巡检白名单同步，不碰 `hkex staged`
  - 把“当前再次适合停手”的判断补回标准交接
- 已完成:
  - `text_report.py` 连续补了最窄过滤，收掉：
    - `stcn` 的券商评论稿、`【早知道】` 摘要拼盘、基金经理配置评论、行业景气综述
    - `stcn` 的互动平台否定式回应，如 `暂未参股CPO、存储芯片类科技公司`
    - `irm_cninfo` 的 `算力基建/后续布局`、`营收/订单大概多少`、`股价与业绩不对称/新项目么` 这类弱问答
    - `sse_einteractive` 的 `大致有多少家` 追问变体
  - `cli.py` 已补 `audit-suspicious` 白名单，同步跳过 `stcn` 的基金经理评论和行业景气综述，避免 report 干净但巡检仍报红
  - `吉利将于2026北京车展发布中国首台原生Robotaxi原型车` 经并行只读确认后保留，不做过滤
- 未完成:
  - 没有继续往下压 `吉利 Robotaxi / 奥特迅 / 长亮科技` 这类 legit 保留样本
  - 没有继续推进 `hkex staged`
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "stcn_csc_broker_macro_commentary_even_if_analysis_has_theme or stcn_early_know_roundup_without_hiding_policy_signal or stcn_fund_manager_allocation_commentary_without_hiding_real_order_news or stcn_industry_prosperity_story_without_hiding_cls_industry_signal or weak_theme_inquiry_replies_without_hiding_substantive_progress or stcn_negative_platform_reply_without_hiding_robotaxi_launch" -q` -> `6 passed`
  - `./.venv/bin/python -m pytest tests/test_audit_suspicious.py -k "stcn_fund_manager_investment_opportunity_story or stcn_industry_prosperity_story" -q` -> `2 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=1781 normalized_news=1781 events=419 analyses=419 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
- 下一步:
  - 先重跑：
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 如果头部仍以 `吉利 Robotaxi / 奥特迅 / 长亮科技` 这类样本为主，先停，不继续压
  - 只有出现新的 `irm_cninfo / stcn / cls` 弱口径样本，再补最窄测试和规则
- 新增/更新的避坑记录:
  - `report` 过滤和 `audit-suspicious` 白名单必须同步看；只收 report 不补巡检，很容易反复看到 `suspicious_count>0`
  - `stcn` 的 `互动平台表示 + 暂未/未参股/未投资/未布局` 更像否定式回应，不要因为题材词面直接保留
  - `吉利 Robotaxi` 这类有明确主体、时间点、动作和技术进展的快讯不要按弱栏目稿误压

### 2026-04-20（phase8 live 边界续收口）
- Task-ID:
  - `phase8-live-boundary`
- Task-Name:
  - `live 样本边界收口 + irm 问答/股权激励材料/cls 栏目稿降噪`
- 本轮目标:
  - 继续按当天 `live-smoke` 头部收掉 `irm_cninfo / 交易所材料 / cls` 的弱样本
  - 只做最窄过滤和必要的 subtype 回正，不碰 `hkex staged`
  - 把当前“已经适合停手”的判断同步进标准交接
- 已完成:
  - `text_report.py` 连续补了最窄过滤，收掉：
    - `irm_cninfo` 的弱问答、回避口径、定期报告导向回复、工商登记进度、低营收占比回复
    - `sse/szse` 的回购、回购注销完成、股权激励、核查意见、风险管理材料、土地合同
    - `cls` 的夜盘综述、盘前要闻、匿名拼盘栏目稿、弱财务收益股权处置稿
    - `海外单股并购快讯 + 无题材无个股`
  - `event_merge/core.py` 已补当前 live 更关键的回正：
    - `省委财经委员会 / 产业引导基金` -> `policy_signal`
    - `乙烯法PVC供应持续收缩` -> `industry_data`
    - `UPC诉讼 / 仲裁裁决` -> `legal_dispute`
    - `美股盘前要闻一览` -> `general_fast_news`
    - `出售/转让股权 + 不构成重大资产重组` 不再误打成 `acquisition_restructuring`
  - 当前头部已基本从弱样本切回 legit 风险公告
- 未完成:
  - 没有继续做 `hkex` staged
  - 没有继续追求“更干净”的最后几条头部，避免过拟合
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_event_merge.py -k "upc_patent_litigation or arbitration_award_challenge or legal_dispute or policy_signal or industry_data or cls_us_pre_market_brief or cls_overnight_roundup" -q` -> `19 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "irm_cninfo or cls_morning_brief or domestic_futures or risk_control_opinion or equity_incentive or order_contract or overseas_single_stock_cls_acquisition" -q` -> `20 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=1766 normalized_news=1766 events=468 analyses=468 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
- 下一步:
  - 先重跑：
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 如果头部仍以 `退市风险 / 撤销风险警示` 为主，先停，不继续压
  - 只有出现新的 `irm_cninfo / equity_incentive / cls` 弱样本再补最窄测试和规则
- 新增/更新的避坑记录:
  - 头部已经回到 legit 风险公告时，继续压头部的收益会明显变小
  - `irm_cninfo` 的弱问答优先在 `text_report` 收口，不急着改上游 subtype
  - `海外单股并购快讯 + 无题材无个股` 不适合留在 A 股主报告头部

### 2026-04-17（hkex staged）
- Task-ID:
  - `phase9-source-expansion`
- Task-Name:
  - `hkex staged 扩源最小分类/过滤收口`
- 本轮目标:
  - 确认 `hkex` 当前是不是已经能并入 `--source all`
  - 先补最小英文 subtype、方向和 report 过滤，不做大范围规则扩张
  - 更新标准交接文件，明确 `hkex` 下一步不是直接启用
- 已完成:
  - 实跑 `collect --source hkex`，当次写出 `1087` 条官方 raw 标题
  - `event_merge` 已补 `hkex` 最小英文 subtype：
    - `PROFIT WARNING / PROFIT ALERT`
    - `CHANGE OF DIRECTORS / RE-DESIGNATION ...`
    - `CONNECTED / MAJOR / VERY SUBSTANTIAL / DISCLOSEABLE TRANSACTION`
    - `INSIDE INFORMATION - UPDATE ON WINDING UP PETITION`
    - `INSIDE INFORMATION ... ARBITRATION PROCEEDINGS`
  - `hkex` 已从通用字符重合归并中摘出，避免英文标题错并
  - `analysis/rules` 已补：
    - `PROFIT WARNING -> bearish`
    - `POSITIVE PROFIT ALERT -> bullish`
    - `WINDING UP PETITION / ARBITRATION PROCEEDINGS -> bearish`
  - `text_report` 已补 `hkex` 英文材料过滤和英文催化词接线
  - 已提交：
    - `2db6b3f` `fix: tighten hkex staged source classification`
- 未完成:
  - `hkex` 还没有 stock code / company mapping
  - `INSIDE INFORMATION` 还没拆成更细上下文子类
  - `hkex` 仍不能直接并入 `--source all`
- 关键命令/结果:
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment collect --source hkex` -> `1087` 条 raw
  - `./.venv/bin/python -m pytest tests/test_event_merge.py -q` -> `51 passed`
  - `./.venv/bin/python -m pytest tests/test_analysis_scoring.py -k "hkex_profit_warning or hkex_positive_profit_alert or delisting_risk or legal_dispute or reorganization_risk" -q` -> `7 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "hkex_" -q` -> `2 passed`
  - `./.venv/bin/python -m pytest tests/test_hkex_collector.py -q` -> `5 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
- 下一步:
  - 先不要启用 `hkex`
  - 若继续扩源，先重跑：
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment collect --source hkex`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment normalize`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment merge-events`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment analyze-events`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment report`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 继续优先补 `INSIDE INFORMATION` 和 stock mapping，再决定要不要并入主链
- 新增/更新的避坑记录:
  - `hkex` 英文标题不能走通用字符重合归并
  - `hkex` 英文材料过滤要做大小写无关匹配
  - `PROFIT WARNING / POSITIVE PROFIT ALERT` 不能继续留在默认 `neutral`

### 2026-04-17（续）
- Task-ID:
  - `phase8-live-boundary`
- Task-Name:
  - `company_update 题材误抬收口 + cls 科技稿/协议材料降噪`
- 本轮目标:
  - 收掉 `企查查成立公司` 造成的 `company_update` 题材误抬
  - 清掉 `cls` 科技特稿和 `股东协议 + 补充协议` 材料尾噪
  - 把“`云天化/恒瑞` 当前先保留”的判断同步进交接文件
- 已完成:
  - `analysis/rules.py` 已补最窄 `company_update` summary spillover 抑制：
    - `企查查APP显示 + 经营范围包含 + 股权穿透显示`
  - `text_report.py` 已补最窄过滤：
    - `cls general_fast_news + 全球首款/研发成功 + 科技日报 + 教授`
    - `sse/szse corporate_disclosure + 股东协议 + 补充协议`
  - 已推远端：
    - `443f22b` `fix: reduce registry company-update theme spillover`
    - `df2fec8` `fix: trim cls science feature story`
    - `8b6dce2` `fix: trim shareholder agreement supplement material`
- 未完成:
  - 没有继续对 `云天化...引入合作方投资建设新能源电池正极材料项目` 下规则
  - 没有继续对 `恒瑞医药...药物纳入突破性治疗品种名单` 下规则
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_analysis_scoring.py -k "company_setup_registry_scope_as_theme or advanced_storage_equipment_as_compute_infra" -q` -> `2 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "cls_science_feature_story_without_hiding_company_product_progress" -q` -> `1 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "exchange_shareholder_agreement_supplement_material_without_hiding_control_change_progress" -q` -> `1 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment report` -> 已重刷 `latest_report.txt`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
- 下一步:
  - 先跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
  - 再跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 只读检查 `华荣股份 / 云天化 / 恒瑞医药` 是否仍在当天头部
  - 若 `云天化` 仍靠前，先补正反样本再决定是否收口
- 新增/更新的避坑记录:
  - `企查查成立公司` 类题材误抬优先改 `analysis/rules.py`，不要先在 `report` 层硬压
  - `cls + general_fast_news + 科技日报/教授` 更像科技特稿，不要按真实公司催化处理
  - `股东协议 + 补充协议` 先按材料簇处理，但不要误伤带结果词的公告

### 2026-04-17
- Task-ID:
  - `phase8-live-boundary`
- Task-Name:
  - `期货夜盘上涨变体收口 + 风险撤销申请方向回正`
- 本轮目标:
  - 收掉 `国内期货夜盘收盘多数上涨 ...` 这条已知同簇漏网变体
  - 查清 `*ST荣控` 为什么仍显示 `bearish`
  - 把最新两刀和当前保留边界补回交接文件
- 已完成:
  - `text_report` 已收掉 `国内期货夜盘收盘多数上涨 甲醇等涨超2%`
  - `analysis/rules` 已把 `申请撤销对公司股票交易实施退市风险警示` 纳入 bullish 风险撤销白名单
  - `*ST荣控...申请撤销对公司股票交易实施退市风险警示的公告` 已从 `bearish` 回正到 `bullish`
  - 已推远端：
    - `a5b129e` `fix: filter futures night-session up roundup`
    - `99d9ccf` `fix: mark delisting warning revocation as bullish`
- 未完成:
  - `华测导航...开展供应链融资业务合作暨对外担保` 仍只做了只读判断，未进入新一轮规则实现
  - `GQY视讯...可能被实施退市风险警示的风险提示公告` 仍按首次风险提示保留
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "domestic_futures_night_session_up_roundup" -q` -> `1 passed`
  - `./.venv/bin/python -m pytest tests/test_analysis_scoring.py -k "delisting_risk_revocation_application" -q` -> `2 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=1259 normalized_news=1259 events=270 analyses=270 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
- 下一步:
  - 先跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
  - 再跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 只读检查 `中远海能 / *ST中基 / *ST荣控 / 华测导航 / GQY视讯` 是否仍在当天头部
- 新增/更新的避坑记录:
  - `国内期货夜盘收盘多数上涨 ...` 是已知同簇 round-up 变体，优先补 `text_report` 词表
  - `申请撤销对公司股票交易实施退市风险警示` 不要被通用 `风险` 词误打成 `bearish`

### 2026-04-16
- Task-ID:
  - `phase8-live-boundary`
- Task-Name:
  - `低信号披露/cls 栏目稿收尾 + 保留边界锁定`
- 本轮目标:
  - 按当天 `live-smoke` 和 `audit-suspicious` 继续清掉低信号披露与栏目稿尾噪
  - 收住 `cninfo corporate_disclosure` 的上游低信号触发
  - 把当前应保留边界写清楚，避免下一轮误伤
- 已完成:
  - `report` 层继续清掉一批低信号样本：
    - `审核问询函回复 / 并购重组材料文档 / 报告书（修订稿）`
    - `解除司法冻结 / 重大诉讼的公告 / 重大诉讼、仲裁情况进展`
    - `增持公司股份计划 / 股份减持完成 / 变更股份回购用途`
    - `停牌核查 + 股票交易风险/风险提示`
    - `【风口研报·洞察】 / 【金牌纪要库】 / 《新闻联播》要闻 / 【电报解读】`
    - `现货白银... / 美股光通信股走势分化 / 再次向港交所提交上市申请书 / 盘后A股上市公司重点业绩公告精选`
  - `analysis/scoring` 已新增最窄上游抑制：
    - `cninfo + hard_event + corporate_disclosure` 中的 `ESG报告 / 业绩网上说明会 / 责任保险` 不再默认触发
  - 已补正样本回归，明确保留：
    - `中远海能...投资建造两艘巴拿马型原油轮暨关联交易`
    - `关于延期披露2025年年度报告及退市风险提示性公告`
    - `【公告全知道】...245亿元投建算电协同项目`
    - `华测导航...开展供应链融资业务合作暨对外担保`
    - `GQY视讯...可能被实施退市风险警示的风险提示公告`
  - 新增提交：
    - `4201aff` `fix: tighten disclosure tail-noise filters`
    - `a71b0ed` `test: lock halt-check and delisting-risk boundaries`
    - `71f5ede` `fix: reduce low-signal disclosure triggering`
    - `97100b5` `fix: filter live report editorial tail noise`
    - `691cd5d` `fix: trim low-signal cls market briefs`
    - `5ac6bf4` `fix: trim low-signal cls digest variants`
- 未完成:
  - `【公告全知道】` 是否还要再单独分层，当前还没进入新一轮实现
  - `华测导航...业务合作暨对外担保` 如果后续重审，还没补 3 条保护测试
  - `hkex` staged 扩源仍未继续
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_analysis_scoring.py -q` -> `45 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "gold_memo_column" -q` -> `1 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "cls_global_market_brief_without_hiding_domestic_order_catalyst" -q` -> `1 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "after_hours_earnings_digest or repeat_hk_listing_application_fast_news" -q` -> `2 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=1235 normalized_news=1235 events=280 analyses=280 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
- 下一步:
  - 先跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
  - 再跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 只读看当天头部是否仍保留 `【公告全知道】 / 华测导航 / GQY视讯` 这 3 类边界
  - 若重审 `华测导航`，先补 `scoring + text_report + event_merge` 三条测试
- 新增/更新的避坑记录:
  - `【公告全知道】` 不是纯编辑尾噪；继续下刀前先确认是否误伤真催化摘要
  - `业务合作 + 对外担保` 不是纯材料簇；不要自动并入授信/担保额度一刀切
  - `repeat hk listing application` 的过滤不要只绑 `stcn`
  - digest 回归要用真实摘要，不要用占位文本

### 2026-04-15（续）
- Task-ID:
  - `phase8-live-boundary`
- Task-Name:
  - `miit 会务回流修复 + cls 全球资讯降层`
- 本轮目标:
  - 重跑当天基线，确认 `cls` 并入主链后没有新漂移
  - 收掉 `miit` 无题材会务回流
  - 将 `cls` 的全球 `industry_data / general_fast_news` 在 report 里再降一档，但不删内容
- 已完成:
  - 重跑真实链路，当前主链仍稳定：
    - `live-smoke --source all` -> `raw_news=1250 normalized_news=1250 events=285 analyses=285 failed_sources=none`
    - `audit-suspicious --limit 10` -> `suspicious_count=0`
  - 在 `report` 层补回 `miit` 会务过滤：
    - `全体会议`
    - `总体组`
    - `咨询组`
  - 在 `report` 层下调 `cls` 的 `industry_data / general_fast_news` 排序优先级，只降层，不删内容
  - 当前头部中的 `2026年国家智能制造标准化总体组和专家咨询组全体会议在京召开` 已退出
- 未完成:
  - `cls` 的 `【风口研报·公司】...` 仍在头部，下一步还没判断是否该单独 subtype
  - `hkex` staged 扩源仍未继续
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "miit_standardization_group_meeting_without_theme or deprioritizes_cls_global_information_below_direct_catalysts" -q` -> `2 passed`
  - `./.venv/bin/python -m pytest tests/test_report_pipeline.py -q` -> `3 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=1250 normalized_news=1250 events=285 analyses=285 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
- 下一步:
  - 先跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
  - 再跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 若头部仍有 `【风口研报·公司】...`，优先判断是否需要把研报/解读稿从真实 `order_contract` 分开
- 新增/更新的避坑记录:
  - `miit` 无题材会务不只会以 `座谈会 / 会见 / 新闻发布会` 形式出现，还会以 `总体组 / 咨询组 / 全体会议` 形式回流
  - `cls industry_data / general_fast_news` 更适合降层，不适合删除

### 2026-04-15
- Task-ID:
  - `phase8-live-boundary`
  - `phase9-source-expansion`
- Task-Name:
  - `cls 并入主链 + live 头部边界收口`
- 本轮目标:
  - 把 `cls` 从 staged 推进到 `--source all`
  - 保留全球快讯覆盖，不再按旧 A 股单线目标压海外内容
  - 持续清掉当天 live 里的交易所材料噪音，并把标准交接文件更新到最新
- 已完成:
  - 新增 `cls` collector，并通过 `__NEXT_DATA__ -> telegraphList` 落地单源抓取
  - `cls` 已启用到 `configs/sources.yaml`，并进入 `--source all`
  - 收住 `cls` 相关边界：
    - `向上触及 / 向下触及` -> `market_move`
    - `隔夜全球要闻 / 新闻精选 / 你需要知道` -> `general_fast_news`
    - `general_fast_news` 强度降到低于 `market_move`，方向改为 `neutral`
    - `audit-suspicious` 不再误打 `cls` 的栏目稿和 `general_fast_news + themes`
  - 主链继续清掉一批交易所材料噪音：
    - `解除限售期解锁暨限制性股票上市公告`
    - `新签合同情况公告`
    - `注销股票期权 / 授予限制性股票 / 相关规定的核查意见`
    - `重整投资协议`
  - `申请撤销公司股票退市风险警示` 已修正为 `bullish`
  - 已更新交接文件：
    - `progress.md`
    - `task_plan.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - 还没进入“头部保留项展示分层”阶段
  - `hkex` 仍未重新进入扩源验收
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_live_smoke.py tests/test_cli_smoke.py tests/test_reference_data.py -q` -> `6 passed`
  - `./.venv/bin/python -m pytest tests/test_cls_collector.py -q` -> `3 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=1298 normalized_news=1298 events=287 analyses=287 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
- 下一步:
  - 先跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
  - 再跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 若头部仍主要是 `control_change / cls general_fast_news / delisting_risk revocation`，先判断是否要做 subtype/强度/展示分层
  - 若切回扩源，再做 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment collect --source hkex`
- 新增/更新的避坑记录:
  - `cls` 已经是主链源，不要再把它当 staged 重复收口
  - 全球快讯覆盖是当前目标的一部分，不要默认把海外/栏目稿重新压掉

### 2026-04-10
- Task-ID:
  - `phase9-source-expansion`
- Task-Name:
  - `第一批官方新闻源扩展`
- 本轮目标:
  - 把扩源底座收成注册表
  - 验证 `csrc` 是否能作为第一条官方政策源接入
  - 保持 `sse` 为 staged source，不拖坏 `--source all`
- 已完成:
  - 将 collector 接入从 `cli.py` 的硬编码分支切到注册表
  - 新增 `sse` collector 骨架，并保持 `enabled: false`
  - 新增 `csrc` collector，并改为抓证监会首页 `新闻发布 -> 证监会要闻` 模块
  - `csrc` 已完成定向测试和单源真实网络验收
- 未完成:
  - `live-smoke --source all` 对 `csrc` 的全链路验收
  - `sse` 官方查询参数验通
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_csrc_collector.py -q` -> `5 passed`
  - `./.venv/bin/python -m pytest tests/test_cli_smoke.py tests/test_live_smoke.py tests/test_report_pipeline.py -q` -> `7 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment collect --source csrc` -> 成功写出 `5` 条样本
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> 用户手动中断，不能视为已验收
- 下一步:
  - 先跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
  - 再跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 若 `csrc` 不稳，先把 `enabled` 临时切回 `false`
- 新增/更新的避坑记录:
  - `csrc` 不要再抓 `c100028/common_xq_list.shtml`
  - `sse` 在真实参数未验通前不要进 `--source all`

### 2026-04-11（续）
- Task-ID:
  - `phase8-live-boundary`
  - `phase9-source-expansion`
- Task-Name:
  - `szse 启用收口 + hkex staged 接入`
- 本轮目标:
  - 把 `szse` 从 staged 推进到主链稳定状态
  - 在不拉低现有 report 信噪比的前提下，为 `hkex` 做 staged 接入和单源验收
  - 更新标准交接文件，统一最新基线和下一步优先级
- 已完成:
  - `szse` 已纳入 `--source all`，并补齐定向测试和主链回归
  - `live-smoke --source all` 已跑通，当前全链路基线更新为 `raw_news=785 normalized_news=785 events=210 analyses=210 failed_sources=none`
  - `audit-suspicious --limit 20` 已回到 `suspicious_count=0`
  - `hkex` collector 已完成官方路径摸底、定向测试和单源真实网络验收
  - 标准交接文件已补齐 `szse 已启用 / hkex staged / 最新验证基线 / 下一步动作`
- 未完成:
  - `hkex` 的最小 report 过滤尚未开始
  - `hkex` 还没有并入 `--source all`
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_szse_collector.py tests/test_cli_smoke.py tests/test_live_smoke.py tests/test_report_pipeline.py -q` -> `12 passed`
  - `./.venv/bin/python -m pytest tests/test_hkex_collector.py -q` -> `5 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=785 normalized_news=785 events=210 analyses=210 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20` -> `suspicious_count=0`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment collect --source hkex` -> 成功写出 `392` 条样本
- 下一步:
  - 先跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment collect --source hkex`
  - 只读扫描 `hkex` 最近标题，归并高噪音标题族
  - 再补最窄 report 过滤，最后才决定是否启用 `hkex`
- 新增/更新的避坑记录:
  - `hkex` 不要直接抓 `lci.html` 表格外壳
  - `hkex` 的真实数据源是官方 `lci.js` 指向的 `/ncms/json/eds/` JSON 分页
  - `report` 过滤和 `audit-suspicious` 不是同一层逻辑，验收时必须同时看两边

### 2026-04-13
- Task-ID:
  - `phase8-live-boundary`
- Task-Name:
  - `live 样本边界收口`
- 本轮目标:
  - 持续按 live 样本清理 report 头部的低信号公告
  - 把 `audit-suspicious` 压回低位，避免材料型公告误报
  - 不扩题材库，只做最窄规则和 subtype 修正
- 已完成:
  - 在 `report` 层清掉多批低信号披露：
    - `hkex` 治理/材料标题族
    - `销售情况简报 / 获得房地产项目 / 谅解备忘录 / 回购贷款承诺函`
    - `回购实施结果 / 回购用途并注销 / 互动易制度 / IPO 投资风险特别公告`
    - `减持结果 / 减持比例触及整数倍 / 预披露 / 实施完成 / 期限届满暨实施情况`
    - `行政处罚决定书 / 监管措施或处罚及整改情况`
    - `可转债信用评级报告 / 询价转让股份核查报告`
    - `基金经理配置评论 / 券商策略评论`
  - 在 `event_merge/core.py` 修正 subtype：
    - `其他风险警示` -> `delisting_risk`
    - `庭外重组` -> `reorganization_risk`
    - `授权许可协议` -> `cooperation_agreement`
  - 在 `audit-suspicious` 清掉材料误报：
    - `可转债审核问询函之回复（修订稿）`
- 未完成:
  - `冻结 / 解除冻结` 主线尚未处理
  - `风险提示公告 / 异常波动暨风险提示 / 实际控制人减持计划期限届满暨实施情况` 仍有残余变体
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "inquiry_transfer_share_verification_report or convertible_bond_credit_rating_report or penalty_and_rectification_disclosures or shareholder_reduction_predisclosure_and_completion_variants" -q` -> `4 passed`
  - `./.venv/bin/python -m pytest tests/test_audit_suspicious.py -k "convertible_bond_inquiry_reply_revision or cninfo_restructuring_material_reply or exchange_inquiry_reply_and_special_explanation or exchange_major_litigation_and_filing_progress_notices" -q` -> `4 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> 最近两次分别为：
    - `raw_news=624 normalized_news=624 events=186 analyses=186 failed_sources=none`
    - `raw_news=429 normalized_news=429 events=160 analyses=160 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20` -> 最近结果滚动为：
    - `suspicious_count=2`（冻结主线）
    - `suspicious_count=0`
- 下一步:
  - 先跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
  - 再跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20`
  - 优先只读判断并处理：
    - `龙元建设...部分银行账户被冻结`
    - `甘咨询...募集资金专户部分资金被冻结`
    - `深圳市龙图光罩...部分自有资金及募集资金解除冻结`
- 新增/更新的避坑记录:
  - 不要沿用上一轮 report 头部清单直接下规则；live 样本日期切换后，头部会换簇
  - `冻结 / 解除冻结` 不能自动视为材料公告；先判断是否是实质风险

### 2026-04-14
- Task-ID:
  - `phase8-live-boundary`
  - `phase9-source-expansion`
- Task-Name:
  - `live 样本边界收口 + 四批提交与远端推送`
- 本轮目标:
  - 把本地未提交改动按可提交语义拆批
  - 继续清理 `live` 样本里的低信号/误分类边界
  - 完成本地提交、远端建仓和分支推送
- 已完成:
  - 收紧并验证多组 `live` 边界：
    - `交易所低信号披露` 变体
    - `合作框架协议` 模板标题
    - `区域统计口径 + 多赛道罗列` 的多题材误挂
    - `申请破产清算` -> `reorganization_risk / bearish`
  - 完成 4 批提交：
    - `0b496f8` `fix: tighten live boundary filtering and risk subtype classification`
    - `def0907` `feat: add official source collectors and suspicious audit workflow`
    - `4948492` `feat: expand narrow theme seeds and reference mappings`
    - `eeb15de` `docs: refresh worktree handoff and task registry`
  - 创建远端仓库：
    - `https://github.com/liutao773680119-cmyk/news-sentiment-analysis`
  - 推送分支：
    - `mvp-foundation -> origin/mvp-foundation`
- 未完成:
  - `hkex` staged 过滤与并入 `--source all` 的主链验收
  - `triggered` 队列里的 `sse/cninfo` 模板材料公告尚未进一步上游收紧
  - 还没有默认分支合并策略、PR 或 release 流程
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_analysis_rules.py tests/test_analysis_scoring.py tests/test_event_merge.py tests/test_text_report_sorting.py -q` -> `233 passed`
  - `./.venv/bin/python -m pytest tests/test_cli_smoke.py tests/test_live_smoke.py tests/test_report_pipeline.py tests/test_csrc_collector.py tests/test_hkex_collector.py tests/test_sse_collector.py tests/test_szse_collector.py tests/test_miit_collector.py tests/test_audit_suspicious.py -q` -> `43 passed`
  - `./.venv/bin/python -m pytest tests/test_mapping_and_history.py tests/test_reference_data.py -q` -> `41 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=719 normalized_news=719 events=183 analyses=183 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20` -> `suspicious_count=0`
  - `gh repo create liutao773680119-cmyk/news-sentiment-analysis --public --description 'A-share news-driven theme radar and sentiment analysis project.'` -> 成功建仓
  - `git -C .worktrees/mvp-foundation push -u origin mvp-foundation` -> 推送成功并建立 upstream
- 下一步:
  - 先跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
  - 再跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20`
  - 若主线继续做扩源，优先 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment collect --source hkex`
  - 若主线继续做规则，优先处理 `triggered` 队列里仍未上游压住的 `sse/cninfo` 模板材料公告
- 新增/更新的避坑记录:
  - 推送前不要假定远端仓库已存在；先 `gh repo view`，不存在再 `gh repo create`
  - `申请破产清算` 不能停留在 `corporate_disclosure / neutral`；应回正到 `reorganization_risk / bearish`

### 2026-04-14（续）
- Task-ID:
  - `phase8-live-boundary`
- Task-Name:
  - `live 样本边界收口`
- 本轮目标:
  - 按当天 `live-smoke` 头部样本继续收紧 `report` 层低信号交易所披露
  - 保持 `audit-suspicious=0`
  - 做一次标准交接，更新 worktree 交接文件
- 已完成:
  - 在 `report` 层继续清掉当天 live 冒出的低信号披露变体：
    - `责任保险`
    - `国债逆回购 / 结构性存款 / 中短期低风险金融理财产品`
    - `减持计划预披露 / 减持计划完成`
    - `股权激励法律意见 / 核查意见 / 注销 / 回购注销 / 自查报告`
    - `持续督导意见 / 受让协议`
    - `中标项目签订协议的进展公告`
    - `模板框架协议`
    - `miit` 的 `巡视动员部署会`
  - 为上述变体新增多组 `tests/test_text_report_sorting.py` 红灯测试，并全部转绿
  - 完成标准交接文件更新：
    - `progress.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - `hkex` staged 过滤与并入 `--source all` 的主链验收仍未开始
  - 当前头部剩余 `盈新发展...控制权变更`、`文科股份...联合中标` 两条，还没判断是否要上游修 subtype/analysis
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -q` -> `116 passed`
  - `./.venv/bin/python -m pytest tests/test_report_pipeline.py -q` -> `3 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> 多次复跑均为 `suspicious_count=0`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> 本轮滚动结果包括：
    - `raw_news=1046 normalized_news=1046 events=236 analyses=236 failed_sources=none`
    - `raw_news=695 normalized_news=695 events=203 analyses=203 failed_sources=none`
    - `raw_news=1046 normalized_news=1046 events=236 analyses=236 failed_sources=none`
- 下一步:
  - 先跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
  - 再跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 若头部仍是 `盈新发展...控制权变更`、`文科股份...联合中标` 一类，优先判断是否需要改 `event_subtype / analysis`，不要继续堆 report 过滤
  - 若主线切回扩源，再按原计划进入 `hkex`
- 新增/更新的避坑记录:
  - 连续清掉交易所材料噪音后，如果头部剩下的是 `control_change / order_contract` 真催化候选，先看上游分类，不要继续在 report 层硬压
# Session Record - 2026-05-15
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `后台 suspicious 尾噪继续收口：新能源车会议与海底数据中心项目示范`
- 本轮目标:
  - 处理后台新出现的 `general_fast_news_with_theme` 可疑信号，并按用户判断降噪。
- 已完成:
  - `三部门召开加强新能源汽车安全管理工作视频会`：
    - 加入 `LOW_SIGNAL_STCN_PUBLIC_AFFAIRS_TITLE_KEYWORDS`
    - 新增 `test_audit_suspicious_skips_stcn_nev_safety_management_video_meeting`
  - `全球首个海底数据中心落户东海`：
    - 新增 `_is_low_signal_stcn_undersea_data_center_story_candidate`
    - 新增 `test_audit_suspicious_skips_stcn_undersea_data_center_demonstration_story`
  - `佳力图拜访之江实验室三体计算星座项目团队 交流液冷散热与太空算力温控技术`：
    - 新增 `_is_low_signal_stcn_company_visit_exchange_story_candidate`
    - 新增 `test_audit_suspicious_skips_stcn_company_visit_exchange_story`
  - 当前 `audit-suspicious --limit 10` 输出 `suspicious_count=0`
  - 后台最近一轮 `2026-05-15_09:10:48` 为 `watchdog_status=clean`、`failed_sources=none`、`suspicious_count=0`
- 未完成:
  - commit + push 尚未完成；本机 `git status` / `git diff` / 部分文件头部读取出现卡住。
  - 定向 pytest / `git diff --check` 命令本轮后半段被文件读取阻塞卡住，需要 Git/文件读取恢复后补跑。
- 关键命令/结果:
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
  - `rg -n '^=====|watchdog_status=|raw_news=|failed_sources=|suspicious_count=' /tmp/news-sentiment-watch.log | tail -n 30` -> 最近轮次 clean
- 下一步:
  - 先清理/确认卡住的只读进程。
  - 补跑定向 pytest。
  - 再执行 `git add`、`git commit -m "fix: suppress latest public affairs audit noise"`、`git push origin HEAD:mvp-foundation`。
- 新增/更新的避坑记录:
  - `海底数据中心落户东海` 只代表项目示范/科普型尾噪，不代表所有数据中心/算力新闻都要降噪。
  - `拜访 + 交流 + 持续推进交流对接/进行了交流` 只代表弱合作线索尾噪；出现 `签署/中标/订单/合同/采购` 时不能套用。

# Session Record - 2026-05-20
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `6小时 watchdog 汇总嵌入 Report Highlights`
- 本轮目标:
  - 让用户只看 6 小时汇总文件，也能看到当前报告重点，不必单独打开 `latest_report.txt`。
- 已完成:
  - `watchdog-summary` 新增 `Report Highlights` 区块。
  - 汇总主动读取 `data/reports/latest_report.txt`。
  - 解析前 10 条 `[关注]` / `[温度]` 报告项。
  - 每条摘要输出类型、来源、方向、强度、题材、个股。
  - README 已补充 6 小时汇总会包含 `Report Highlights`。
  - 已生成样例：
    - `data/monitoring/summaries/20260520T023731Z-summary.md`
- 未完成:
  - 尚未提交推送本轮交接与实现改动。
  - 下一轮后台自动汇总还需等实际 6 小时调度自然生成后再只读确认。
- 关键命令/结果:
  - `tests/test_watchdog_summary.py::test_watchdog_summary_embeds_latest_report_highlights -q` -> 先红灯失败，缺少 `## Report Highlights`
  - `tests/test_watchdog_summary.py::test_watchdog_summary_embeds_latest_report_highlights -q` -> `1 passed`
  - `tests/test_watchdog_summary.py tests/test_readme_commands.py tests/test_watchdog_loop_script.py -q` -> `6 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment watchdog-summary --hours 6` -> 生成 `20260520T023731Z-summary.md`
  - `py_compile src/news_sentiment/watchdog_summary.py tests/test_watchdog_summary.py` -> passed
  - `git diff --check` -> passed
- 下一步:
  - 提交并推送本轮变更。
  - 推送后用：
    - `sed -n '/## Report Highlights/,+80p' data/monitoring/summaries/$(ls -1 data/monitoring/summaries/*-summary.md | tail -1)`
  - 等下一轮自动 6 小时汇总生成后再确认其带有 `Report Highlights`。
- 新增/更新的避坑记录:
  - `Report Highlights` 是当前最新报告摘要，不是完整报告，也不是 6 小时历史回放。
  - 6 小时汇总仍只展示巡检和报告重点，不自动改规则。
