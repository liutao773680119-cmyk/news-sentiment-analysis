# 修改记录

用于记录“某一轮会话具体做了什么、停在什么地方”。  
这里按会话顺序追加；长期结论仍以 `progress.md / findings.md` 为准。

## Session Record

### 2026-04-22（global multisource report 分层骨架）
- Task-ID:
  - `global-multisource-mainline`
- Task-Name:
  - `全球多源主线 report 分层骨架 + sidecar 尾部保持`
- 本轮目标:
  - 把“全球多源主线”从文档目标变成实际报告结构
  - 只改展示层，不动评分和主链路边界
- 已完成:
  - `text_report` 已从单榜混排改成 4 层输出：
    - `A股强催化`
    - `国内政策与监管`
    - `全球政策与监管`
    - `全球市场与商品`
  - `社交热度观察` 仍保留在主报告所有层之后，没有并入主榜
  - 已同步更新：
    - `progress.md`
    - `task_plan.md`
    - `findings.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - 没有开始调各层内部排序
  - 没有新增层摘要/层说明
  - 没有提交
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -q -k 'groups_entries_into_global_multisource_sections or keeps_social_section_after_mainline_sections'` -> `2 passed`
  - `./.venv/bin/python -m pytest tests/test_report_pipeline.py -q` -> `4 passed`
  - `./.venv/bin/python -m pytest tests/test_cli_smoke.py -q` -> `2 passed`
- 下一步:
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment run-once --source all`
  - 先看分层后的层内排序，再决定是否要补最窄过滤

### 2026-04-22（global multisource + social sidecar 标准交接）
- Task-ID:
  - `global-multisource-mainline`
  - `social-sidecar`
- Task-Name:
  - `全球多源主线边界重定标 + 社交 sidecar 最小接线 + 标准交接`
- 本轮目标:
  - 把当前主线从旧 `phase8` 口径切回“全球多源主线”
  - 给社交源补最小 sidecar 闭环，但不改主评分主链路
  - 把当前真实边界、验证结果和下一步同步进标准交接文件
- 已完成:
  - 已把当前主线明确为 `global-multisource-mainline`，不再沿用旧 `phase8-live-boundary` A股单线验收
  - 已补社交 sidecar 最小闭环：
    - `SocialSignal` 数据结构
    - `data/social/social_signals.jsonl`
    - `collect-social --platform fixture|weibo`
    - report 底部 `社交热度观察`
  - 已补 `collect-social` 失败可见性：
    - `weibo` 被访客门/403 挡住时，输出 `warning: social_platform_failed=weibo:fetch_error:...`
    - 退出码仍保持 `0`，不打断主线脚本
  - 已同步更新：
    - `README.md`
    - `progress.md`
    - `task_plan.md`
    - `findings.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - 没有开始做 report 分层骨架
  - 没有把社交热度接进 `analysis/scoring`
  - 没有证明 `weibo` 生产可用
  - 没有提交
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_social_collectors.py -q` -> `3 passed`
  - `./.venv/bin/python -m pytest tests/test_report_pipeline.py -q` -> `4 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -q -k social_signal_section_without_affecting_main_entries` -> `1 passed`
  - `./.venv/bin/python -m pytest tests/test_report_pipeline.py -q -k social` -> `1 passed`
- 下一步:
  - 若继续主线：
    - `sed -n '1,260p' src/news_sentiment/reporting/text_report.py`
    - 先做 report 分层骨架，不要先补更多过滤
  - 若继续社交 sidecar：
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment collect-social --platform fixture`
    - 先验证展示落点，再评估稳定公开入口；不要直接并入主评分
  - 若准备收尾提交：
    - `git status --short`
    - `git add README.md progress.md task_plan.md findings.md task_registry.md 修改记录_会话备忘.md 避坑记录.md src/news_sentiment/cli.py src/news_sentiment/models.py src/news_sentiment/settings.py src/news_sentiment/reporting/text_report.py src/news_sentiment/social_collectors.py tests/test_report_pipeline.py tests/test_text_report_sorting.py tests/test_social_collectors.py docs/plans/2026-04-22-social-signal-sidecar-design.md`
    - `git commit`
- 新增/更新的避坑记录:
  - 社交源当前只做 sidecar，不进主评分
  - `weibo` 当前 visitor gate/403 仍是外部限制，不要误判成主链路 bug
  - 当前更值得优先做的是 report 分层，不是继续沿旧 `phase8` 口径压头部

### 2026-04-22（phase9 hkex staged 标准交接）
- Task-ID:
  - `phase9-source-expansion`
- Task-Name:
  - `hkex staged 英文材料标题最窄过滤续收口 + 标准交接`
- 本轮目标:
  - 把当前 `hkex staged` 的停手点、剩余样本判断和下一步落盘
  - 不再继续改业务规则
  - 让下一个会话可直接决定“提交”还是“继续单源审查”
- 已完成:
  - 已把 `Latest Handoff Snapshot` 更新为当前真实状态
  - 已同步更新：
    - `progress.md`
    - `task_plan.md`
    - `findings.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
  - 已明确当前停手判断：
    - `CONNECTED TRANSACTION - ACQUISITION OF SOFTWARE ASSETS`
    - `CONNECTED TRANSACTION ENTERING INTO THE CAPITAL INCREASE AGREEMENT`
    - `DISCLOSEABLE TRANSACTION: FURTHER INVESTMENT IN PRECIOUS METALS`
    这 3 条当前更像真实交易主标题，先不继续压
- 未完成:
  - 没有提交
  - 没有继续判断这 3 条是否还要下更细规则
- 关键命令/结果:
  - `git status --short` -> 当前仍有业务代码和交接文档未提交
  - `git branch --show-current` -> `mvp-foundation`
- 下一步:
  - 若继续审查：
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment run-once --source hkex`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20`
  - 若准备收尾提交：
    - `git status --short`
    - `git add src/news_sentiment/reporting/text_report.py tests/test_text_report_sorting.py progress.md task_plan.md findings.md task_registry.md 修改记录_会话备忘.md 避坑记录.md`
    - `git commit`
- 新增/更新的避坑记录:
  - `hkex staged` 收到只剩交易主标题时，优先停手交接，不要继续为了“更干净”过拟合
  - 标准交接要把“当前不该再砍的标题”明确写进交接文件，避免下一会话又把它们当噪音下刀

### 2026-04-22（phase9 hkex staged 最窄收口）
- Task-ID:
  - `phase9-source-expansion`
- Task-Name:
  - `hkex staged 英文材料标题最窄过滤续收口`
- 本轮目标:
  - 不开 `hkex` 到 `--source all`
  - 只沿 `run-once --source hkex` 头部连续补最窄回归和过滤
  - 到剩余头部更像交易主标题时及时停手并落盘交接
- 已完成:
  - `tests/test_text_report_sorting.py` 新增一组 `hkex_...` 回归，累计到 `13` 条
  - `text_report.py` 继续补最窄 `hkex` 过滤，收掉：
    - `performance undertaking / results update + convertible bonds accounting treatment / resumption guidance`
    - `AGM/EGM + transaction`
    - `delay in despatch of circular / extension of proposed completion date`
    - `director share acquisition / supplemental announcement / annual caps`
    - `maintenance work contracts / service framework agreement / leasing and licensing framework agreement`
    - `purchase agreements + sales agreements`
    - `results/management accounts + continued suspension of trading`
    - `voluntary announcement acquisition of assets`
    - `connected transaction + continuing connected transaction + lease agreement`
  - 多轮 `run-once --source hkex` 重刷后，当前头部里已主要剩下更像真实交易主标题的样本：
    - `CONNECTED TRANSACTION - ACQUISITION OF SOFTWARE ASSETS`
    - `CONNECTED TRANSACTION ENTERING INTO THE CAPITAL INCREASE AGREEMENT`
    - `DISCLOSEABLE TRANSACTION: FURTHER INVESTMENT IN PRECIOUS METALS`
- 未完成:
  - 没有继续判断这 3 条交易主标题是否还要压
  - 没有把 `hkex` 开进 `--source all`
  - 没有提交
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -q -k 'hkex_'` -> `13 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment run-once --source hkex` -> 已多轮重刷
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20` -> `suspicious_count=0`
- 下一步:
  - 先重跑：
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment run-once --source hkex`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20`
  - 如果头部仍主要是 `software assets / capital increase / precious metals` 这类标题，优先停手，不继续压
  - 只有出现新的明显材料族，才继续补最窄规则
- 新增/更新的避坑记录:
  - `hkex` 这轮不要再去动 `event_merge / analysis`；最有效的刀仍是 `text_report` 词面过滤
  - `CONNECTED / DISCLOSEABLE / MAJOR TRANSACTION` 主干不是默认低信号；要盯材料尾词而不是交易主干
  - `run-once --source hkex` 当前常没有 stdout，验收要直接看 `latest_report.txt`

### 2026-04-21（phase8 live 基线复跑 + 错并修复 + 新漏样收口）
- Task-ID:
  - `phase8-live-boundary`
- Task-Name:
  - `live 样本边界收口 + 交易所同模板错并修复 + irm/equity_incentive 新漏样收口`
- 本轮目标:
  - 先按交接重跑当天 `live-smoke` / `audit-suspicious`
  - 如果没有新弱样本，就停；如果冒出真问题，优先修根因，不继续盲压 report
  - 把本轮修到的错并和新漏样一起补回标准交接
- 已完成:
  - 复跑基线并确认 `audit-suspicious --limit 10` 仍为 `0`
  - 只读定位到 `szse` 同模板退市风险公告错并：`奥特迅 / *ST声迅 / 明德生物 / ST赛为` 被错误合成一个 event
  - `event_merge/core.py` 已修：
    - `hard_event` 先按结构化催化和股票代码判断
    - 股票代码提取补了 `news_id` fallback
    - 四条退市风险公告已从一个错并 event 拆回四条独立 event
  - 继续按新头部收口：
    - `irm_cninfo` 的 question-only 弱问答 `新雷能 / 山东威达` 已按最窄 title-only 规则过滤
    - `szse equity_incentive` 的 `限制性股票激励计划有关事项的核查意见` 已补精确词面过滤
    - `cls` 的单票跌停回应稿 `股价“一”字跌停 英维克最新回应` 已按 `market_move + 无题材 + 回应解释口径` 过滤
    - 当天新冒头的一组 `irm_cninfo` 弱回复也已按最窄 title+reply 规则过滤：
      - `协创数据` 的经营范围介绍 / 订单充裕口径
      - `协鑫集成` 的“无未披露重大资产重组”回复
      - `通化金马` 的“密切关注 + 审慎论证”
      - `宏明电子` 的并购方向泛回复 / 小批量供货但收入占比较小
      - `大华股份` 的减持预披露规则追问
      - `海康威视` 的继续回购诉求
      - `茂化实华` 的定增正常推进回复
    - `cls` 的 `三大指数全部翻红` 已按 A 股指数综述变体过滤
    - `stcn` 的 `协创数据：2026年将持续加大算力业务投入 目前在手订单充裕` 已按互动平台经营口径转发过滤
  - 已重刷：
    - `merge-events`
    - `report`
    - 当前 `latest_report.txt` 里 `新雷能 / 山东威达 / 光大同创 / 英维克最新回应 / 协创数据 / 协鑫集成 / 宏明电子 / 通化金马 / 大华股份 / 海康威视 / 茂化实华` 已退出
- 未完成:
  - 没有继续往下压当前新的 `cls/stcn` 市场异动头部
  - 没有继续推进 `hkex staged`
  - 没有做提交
- 关键命令/结果:
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
  - `./.venv/bin/python -m pytest tests/test_event_merge.py -k "different_szse_delisting_risk_notices" -q` -> `1 passed`
  - `./.venv/bin/python -m pytest tests/test_event_merge.py -k "groups_structured_cninfo_catalyst_documents or classifies_delisting_risk_notice_subtype or classifies_other_risk_warning_notice_subtype" -q` -> `3 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "question_only_titles or committee_verification_notice" -q` -> `2 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "compute_infra_fallback or disclosure_threshold_and_low_revenue_replies or exchange_equity_incentive_audit_materials_without_theme or current_live_equity_incentive_unmet_exercise_condition_notice" -q` -> `3 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "cls_single_stock_limit_down_response_without_theme" -q` -> `1 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "keeps_cls_single_stock_market_move_for_global_news_collection or robot_half_marathon_supply_chain_story_without_hiding_robot_order or question_only_titles or committee_verification_notice" -q` -> `4 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "current_live_irm_weak_replies_without_hiding_substantive_progress" -q` -> `1 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "current_live_irm_question_only_titles_without_hiding_substantive_progress or irm_cninfo_disclosure_threshold_and_low_revenue_replies_without_hiding_substantive_progress or compute_infra_fallback or cls_single_stock_limit_down_response_without_theme" -q` -> `3 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "cls_three_major_indices_roundup_variant or stcn_interactive_order_plenty_update_without_hiding_substantive_order" -q` -> `2 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "filters_single_ashare_index_roundup_fast_news or filters_stcn_operational_update_with_stable_order_wording or keeps_cls_single_stock_market_move_for_global_news_collection or robot_half_marathon_supply_chain_story_without_hiding_robot_order" -q` -> `4 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment merge-events` -> `events.jsonl` 已反映拆分后的四条退市风险公告
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment report` 与直接 `PYTHONPATH=src` 调 `write_text_report()` -> `新雷能 / 山东威达 / 光大同创 / 英维克最新回应 / 协创数据 / 协鑫集成 / 宏明电子 / 通化金马 / 大华股份 / 海康威视 / 茂化实华 / 三大指数全部翻红 / 协创数据：2026年将持续加大算力业务投入 目前在手订单充裕` 不再出现在 `latest_report.txt`
- 下一步:
  - 先重跑：
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 只读判断后，当前剩余头部里的 `乌克兰机器人扩单 / WTI跌破86 / 世界人形机器人运动会` 更像 legit 保留样本
  - 如果下一轮头部仍主要是这类样本，优先停手，不继续过拟合
- 新增/更新的避坑记录:
  - `hard_event` 同模板交易所公告不能只靠标题相似度归并，必须先看股票代码
  - 修完 `event_merge` 后如果不重跑 `merge-events`，`events.jsonl` 会继续保留旧错并结果
  - `irm_cninfo` 当前 live 有 question-only 样本，不能继续假设一定有回复正文
  - `cls` 的单票跌停回应样本不要因为 `跌停` 就保留；如果是 `无题材 + 回应解释口径`，优先在 `text_report` 窄口径过滤
  - `report` 命令在当前环境里有时看起来执行成功，但 `latest_report.txt` 不一定按最新源码重算；要直接核对目标样本是否退出，必要时用 `PYTHONPATH=src` 调 `write_text_report()` 强制刷新
  - `三大指数全部翻红` 这类标题本身看着像弱综述，但真正把它抬进 report 的常见原因是正文里混入 `跌超1%` 之类 `market_move` 宽词；优先在 `text_report` 按“指数综述变体”过滤，不要误判成主题识别问题
  - `cls` 当前 global news 口径下，`WTI原油期货` 异动和部分海外主题快讯是有意保留的；不要因为它们不是 A 股单票催化就顺手清掉

### 2026-04-21（phase8 live 边界续收口）
- Task-ID:
  - `phase8-live-boundary`
- Task-Name:
  - `live 样本边界收口 + irm 问答/stcn 栏目稿/否定式回应降噪`
- 本轮目标:
  - 继续按当天 `live-smoke` 头部收掉 `stcn` 券商评论/摘要拼盘和 `irm_cninfo` 新弱问答
  - 只做最窄过滤和必要的巡检白名单同步，不碰 `hkex staged`
  - 把“当前再次适合停手”的判断补回标准交接
- 已完成:
  - `text_report.py` 连续补了最窄过滤，收掉：
    - `stcn` 的券商评论稿、`【早知道】` 摘要拼盘、基金经理配置评论、行业景气综述
    - `stcn` 的互动平台否定式回应，如 `暂未参股CPO、存储芯片类科技公司`
    - `irm_cninfo` 的 `算力基建/后续布局`、`营收/订单大概多少`、`股价与业绩不对称/新项目么` 这类弱问答
    - `sse_einteractive` 的 `大致有多少家` 追问变体
  - `cli.py` 已补 `audit-suspicious` 白名单，同步跳过 `stcn` 的基金经理评论和行业景气综述，避免 report 干净但巡检仍报红
  - `吉利将于2026北京车展发布中国首台原生Robotaxi原型车` 经并行只读确认后保留，不做过滤
- 未完成:
  - 没有继续往下压 `吉利 Robotaxi / 奥特迅 / 长亮科技` 这类 legit 保留样本
  - 没有继续推进 `hkex staged`
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "stcn_csc_broker_macro_commentary_even_if_analysis_has_theme or stcn_early_know_roundup_without_hiding_policy_signal or stcn_fund_manager_allocation_commentary_without_hiding_real_order_news or stcn_industry_prosperity_story_without_hiding_cls_industry_signal or weak_theme_inquiry_replies_without_hiding_substantive_progress or stcn_negative_platform_reply_without_hiding_robotaxi_launch" -q` -> `6 passed`
  - `./.venv/bin/python -m pytest tests/test_audit_suspicious.py -k "stcn_fund_manager_investment_opportunity_story or stcn_industry_prosperity_story" -q` -> `2 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=1781 normalized_news=1781 events=419 analyses=419 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
- 下一步:
  - 先重跑：
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 如果头部仍以 `吉利 Robotaxi / 奥特迅 / 长亮科技` 这类样本为主，先停，不继续压
  - 只有出现新的 `irm_cninfo / stcn / cls` 弱口径样本，再补最窄测试和规则
- 新增/更新的避坑记录:
  - `report` 过滤和 `audit-suspicious` 白名单必须同步看；只收 report 不补巡检，很容易反复看到 `suspicious_count>0`
  - `stcn` 的 `互动平台表示 + 暂未/未参股/未投资/未布局` 更像否定式回应，不要因为题材词面直接保留
  - `吉利 Robotaxi` 这类有明确主体、时间点、动作和技术进展的快讯不要按弱栏目稿误压

### 2026-04-20（phase8 live 边界续收口）
- Task-ID:
  - `phase8-live-boundary`
- Task-Name:
  - `live 样本边界收口 + irm 问答/股权激励材料/cls 栏目稿降噪`
- 本轮目标:
  - 继续按当天 `live-smoke` 头部收掉 `irm_cninfo / 交易所材料 / cls` 的弱样本
  - 只做最窄过滤和必要的 subtype 回正，不碰 `hkex staged`
  - 把当前“已经适合停手”的判断同步进标准交接
- 已完成:
  - `text_report.py` 连续补了最窄过滤，收掉：
    - `irm_cninfo` 的弱问答、回避口径、定期报告导向回复、工商登记进度、低营收占比回复
    - `sse/szse` 的回购、回购注销完成、股权激励、核查意见、风险管理材料、土地合同
    - `cls` 的夜盘综述、盘前要闻、匿名拼盘栏目稿、弱财务收益股权处置稿
    - `海外单股并购快讯 + 无题材无个股`
  - `event_merge/core.py` 已补当前 live 更关键的回正：
    - `省委财经委员会 / 产业引导基金` -> `policy_signal`
    - `乙烯法PVC供应持续收缩` -> `industry_data`
    - `UPC诉讼 / 仲裁裁决` -> `legal_dispute`
    - `美股盘前要闻一览` -> `general_fast_news`
    - `出售/转让股权 + 不构成重大资产重组` 不再误打成 `acquisition_restructuring`
  - 当前头部已基本从弱样本切回 legit 风险公告
- 未完成:
  - 没有继续做 `hkex` staged
  - 没有继续追求“更干净”的最后几条头部，避免过拟合
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_event_merge.py -k "upc_patent_litigation or arbitration_award_challenge or legal_dispute or policy_signal or industry_data or cls_us_pre_market_brief or cls_overnight_roundup" -q` -> `19 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "irm_cninfo or cls_morning_brief or domestic_futures or risk_control_opinion or equity_incentive or order_contract or overseas_single_stock_cls_acquisition" -q` -> `20 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=1766 normalized_news=1766 events=468 analyses=468 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
- 下一步:
  - 先重跑：
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 如果头部仍以 `退市风险 / 撤销风险警示` 为主，先停，不继续压
  - 只有出现新的 `irm_cninfo / equity_incentive / cls` 弱样本再补最窄测试和规则
- 新增/更新的避坑记录:
  - 头部已经回到 legit 风险公告时，继续压头部的收益会明显变小
  - `irm_cninfo` 的弱问答优先在 `text_report` 收口，不急着改上游 subtype
  - `海外单股并购快讯 + 无题材无个股` 不适合留在 A 股主报告头部

### 2026-04-17（hkex staged）
- Task-ID:
  - `phase9-source-expansion`
- Task-Name:
  - `hkex staged 扩源最小分类/过滤收口`
- 本轮目标:
  - 确认 `hkex` 当前是不是已经能并入 `--source all`
  - 先补最小英文 subtype、方向和 report 过滤，不做大范围规则扩张
  - 更新标准交接文件，明确 `hkex` 下一步不是直接启用
- 已完成:
  - 实跑 `collect --source hkex`，当次写出 `1087` 条官方 raw 标题
  - `event_merge` 已补 `hkex` 最小英文 subtype：
    - `PROFIT WARNING / PROFIT ALERT`
    - `CHANGE OF DIRECTORS / RE-DESIGNATION ...`
    - `CONNECTED / MAJOR / VERY SUBSTANTIAL / DISCLOSEABLE TRANSACTION`
    - `INSIDE INFORMATION - UPDATE ON WINDING UP PETITION`
    - `INSIDE INFORMATION ... ARBITRATION PROCEEDINGS`
  - `hkex` 已从通用字符重合归并中摘出，避免英文标题错并
  - `analysis/rules` 已补：
    - `PROFIT WARNING -> bearish`
    - `POSITIVE PROFIT ALERT -> bullish`
    - `WINDING UP PETITION / ARBITRATION PROCEEDINGS -> bearish`
  - `text_report` 已补 `hkex` 英文材料过滤和英文催化词接线
  - 已提交：
    - `2db6b3f` `fix: tighten hkex staged source classification`
- 未完成:
  - `hkex` 还没有 stock code / company mapping
  - `INSIDE INFORMATION` 还没拆成更细上下文子类
  - `hkex` 仍不能直接并入 `--source all`
- 关键命令/结果:
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment collect --source hkex` -> `1087` 条 raw
  - `./.venv/bin/python -m pytest tests/test_event_merge.py -q` -> `51 passed`
  - `./.venv/bin/python -m pytest tests/test_analysis_scoring.py -k "hkex_profit_warning or hkex_positive_profit_alert or delisting_risk or legal_dispute or reorganization_risk" -q` -> `7 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "hkex_" -q` -> `2 passed`
  - `./.venv/bin/python -m pytest tests/test_hkex_collector.py -q` -> `5 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
- 下一步:
  - 先不要启用 `hkex`
  - 若继续扩源，先重跑：
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment collect --source hkex`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment normalize`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment merge-events`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment analyze-events`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment report`
    - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 继续优先补 `INSIDE INFORMATION` 和 stock mapping，再决定要不要并入主链
- 新增/更新的避坑记录:
  - `hkex` 英文标题不能走通用字符重合归并
  - `hkex` 英文材料过滤要做大小写无关匹配
  - `PROFIT WARNING / POSITIVE PROFIT ALERT` 不能继续留在默认 `neutral`

### 2026-04-17（续）
- Task-ID:
  - `phase8-live-boundary`
- Task-Name:
  - `company_update 题材误抬收口 + cls 科技稿/协议材料降噪`
- 本轮目标:
  - 收掉 `企查查成立公司` 造成的 `company_update` 题材误抬
  - 清掉 `cls` 科技特稿和 `股东协议 + 补充协议` 材料尾噪
  - 把“`云天化/恒瑞` 当前先保留”的判断同步进交接文件
- 已完成:
  - `analysis/rules.py` 已补最窄 `company_update` summary spillover 抑制：
    - `企查查APP显示 + 经营范围包含 + 股权穿透显示`
  - `text_report.py` 已补最窄过滤：
    - `cls general_fast_news + 全球首款/研发成功 + 科技日报 + 教授`
    - `sse/szse corporate_disclosure + 股东协议 + 补充协议`
  - 已推远端：
    - `443f22b` `fix: reduce registry company-update theme spillover`
    - `df2fec8` `fix: trim cls science feature story`
    - `8b6dce2` `fix: trim shareholder agreement supplement material`
- 未完成:
  - 没有继续对 `云天化...引入合作方投资建设新能源电池正极材料项目` 下规则
  - 没有继续对 `恒瑞医药...药物纳入突破性治疗品种名单` 下规则
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_analysis_scoring.py -k "company_setup_registry_scope_as_theme or advanced_storage_equipment_as_compute_infra" -q` -> `2 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "cls_science_feature_story_without_hiding_company_product_progress" -q` -> `1 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "exchange_shareholder_agreement_supplement_material_without_hiding_control_change_progress" -q` -> `1 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment report` -> 已重刷 `latest_report.txt`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
- 下一步:
  - 先跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
  - 再跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 只读检查 `华荣股份 / 云天化 / 恒瑞医药` 是否仍在当天头部
  - 若 `云天化` 仍靠前，先补正反样本再决定是否收口
- 新增/更新的避坑记录:
  - `企查查成立公司` 类题材误抬优先改 `analysis/rules.py`，不要先在 `report` 层硬压
  - `cls + general_fast_news + 科技日报/教授` 更像科技特稿，不要按真实公司催化处理
  - `股东协议 + 补充协议` 先按材料簇处理，但不要误伤带结果词的公告

### 2026-04-17
- Task-ID:
  - `phase8-live-boundary`
- Task-Name:
  - `期货夜盘上涨变体收口 + 风险撤销申请方向回正`
- 本轮目标:
  - 收掉 `国内期货夜盘收盘多数上涨 ...` 这条已知同簇漏网变体
  - 查清 `*ST荣控` 为什么仍显示 `bearish`
  - 把最新两刀和当前保留边界补回交接文件
- 已完成:
  - `text_report` 已收掉 `国内期货夜盘收盘多数上涨 甲醇等涨超2%`
  - `analysis/rules` 已把 `申请撤销对公司股票交易实施退市风险警示` 纳入 bullish 风险撤销白名单
  - `*ST荣控...申请撤销对公司股票交易实施退市风险警示的公告` 已从 `bearish` 回正到 `bullish`
  - 已推远端：
    - `a5b129e` `fix: filter futures night-session up roundup`
    - `99d9ccf` `fix: mark delisting warning revocation as bullish`
- 未完成:
  - `华测导航...开展供应链融资业务合作暨对外担保` 仍只做了只读判断，未进入新一轮规则实现
  - `GQY视讯...可能被实施退市风险警示的风险提示公告` 仍按首次风险提示保留
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "domestic_futures_night_session_up_roundup" -q` -> `1 passed`
  - `./.venv/bin/python -m pytest tests/test_analysis_scoring.py -k "delisting_risk_revocation_application" -q` -> `2 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=1259 normalized_news=1259 events=270 analyses=270 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
- 下一步:
  - 先跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
  - 再跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 只读检查 `中远海能 / *ST中基 / *ST荣控 / 华测导航 / GQY视讯` 是否仍在当天头部
- 新增/更新的避坑记录:
  - `国内期货夜盘收盘多数上涨 ...` 是已知同簇 round-up 变体，优先补 `text_report` 词表
  - `申请撤销对公司股票交易实施退市风险警示` 不要被通用 `风险` 词误打成 `bearish`

### 2026-04-16
- Task-ID:
  - `phase8-live-boundary`
- Task-Name:
  - `低信号披露/cls 栏目稿收尾 + 保留边界锁定`
- 本轮目标:
  - 按当天 `live-smoke` 和 `audit-suspicious` 继续清掉低信号披露与栏目稿尾噪
  - 收住 `cninfo corporate_disclosure` 的上游低信号触发
  - 把当前应保留边界写清楚，避免下一轮误伤
- 已完成:
  - `report` 层继续清掉一批低信号样本：
    - `审核问询函回复 / 并购重组材料文档 / 报告书（修订稿）`
    - `解除司法冻结 / 重大诉讼的公告 / 重大诉讼、仲裁情况进展`
    - `增持公司股份计划 / 股份减持完成 / 变更股份回购用途`
    - `停牌核查 + 股票交易风险/风险提示`
    - `【风口研报·洞察】 / 【金牌纪要库】 / 《新闻联播》要闻 / 【电报解读】`
    - `现货白银... / 美股光通信股走势分化 / 再次向港交所提交上市申请书 / 盘后A股上市公司重点业绩公告精选`
  - `analysis/scoring` 已新增最窄上游抑制：
    - `cninfo + hard_event + corporate_disclosure` 中的 `ESG报告 / 业绩网上说明会 / 责任保险` 不再默认触发
  - 已补正样本回归，明确保留：
    - `中远海能...投资建造两艘巴拿马型原油轮暨关联交易`
    - `关于延期披露2025年年度报告及退市风险提示性公告`
    - `【公告全知道】...245亿元投建算电协同项目`
    - `华测导航...开展供应链融资业务合作暨对外担保`
    - `GQY视讯...可能被实施退市风险警示的风险提示公告`
  - 新增提交：
    - `4201aff` `fix: tighten disclosure tail-noise filters`
    - `a71b0ed` `test: lock halt-check and delisting-risk boundaries`
    - `71f5ede` `fix: reduce low-signal disclosure triggering`
    - `97100b5` `fix: filter live report editorial tail noise`
    - `691cd5d` `fix: trim low-signal cls market briefs`
    - `5ac6bf4` `fix: trim low-signal cls digest variants`
- 未完成:
  - `【公告全知道】` 是否还要再单独分层，当前还没进入新一轮实现
  - `华测导航...业务合作暨对外担保` 如果后续重审，还没补 3 条保护测试
  - `hkex` staged 扩源仍未继续
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_analysis_scoring.py -q` -> `45 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "gold_memo_column" -q` -> `1 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "cls_global_market_brief_without_hiding_domestic_order_catalyst" -q` -> `1 passed`
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "after_hours_earnings_digest or repeat_hk_listing_application_fast_news" -q` -> `2 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=1235 normalized_news=1235 events=280 analyses=280 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
- 下一步:
  - 先跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
  - 再跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 只读看当天头部是否仍保留 `【公告全知道】 / 华测导航 / GQY视讯` 这 3 类边界
  - 若重审 `华测导航`，先补 `scoring + text_report + event_merge` 三条测试
- 新增/更新的避坑记录:
  - `【公告全知道】` 不是纯编辑尾噪；继续下刀前先确认是否误伤真催化摘要
  - `业务合作 + 对外担保` 不是纯材料簇；不要自动并入授信/担保额度一刀切
  - `repeat hk listing application` 的过滤不要只绑 `stcn`
  - digest 回归要用真实摘要，不要用占位文本

### 2026-04-15（续）
- Task-ID:
  - `phase8-live-boundary`
- Task-Name:
  - `miit 会务回流修复 + cls 全球资讯降层`
- 本轮目标:
  - 重跑当天基线，确认 `cls` 并入主链后没有新漂移
  - 收掉 `miit` 无题材会务回流
  - 将 `cls` 的全球 `industry_data / general_fast_news` 在 report 里再降一档，但不删内容
- 已完成:
  - 重跑真实链路，当前主链仍稳定：
    - `live-smoke --source all` -> `raw_news=1250 normalized_news=1250 events=285 analyses=285 failed_sources=none`
    - `audit-suspicious --limit 10` -> `suspicious_count=0`
  - 在 `report` 层补回 `miit` 会务过滤：
    - `全体会议`
    - `总体组`
    - `咨询组`
  - 在 `report` 层下调 `cls` 的 `industry_data / general_fast_news` 排序优先级，只降层，不删内容
  - 当前头部中的 `2026年国家智能制造标准化总体组和专家咨询组全体会议在京召开` 已退出
- 未完成:
  - `cls` 的 `【风口研报·公司】...` 仍在头部，下一步还没判断是否该单独 subtype
  - `hkex` staged 扩源仍未继续
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "miit_standardization_group_meeting_without_theme or deprioritizes_cls_global_information_below_direct_catalysts" -q` -> `2 passed`
  - `./.venv/bin/python -m pytest tests/test_report_pipeline.py -q` -> `3 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=1250 normalized_news=1250 events=285 analyses=285 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
- 下一步:
  - 先跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
  - 再跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 若头部仍有 `【风口研报·公司】...`，优先判断是否需要把研报/解读稿从真实 `order_contract` 分开
- 新增/更新的避坑记录:
  - `miit` 无题材会务不只会以 `座谈会 / 会见 / 新闻发布会` 形式出现，还会以 `总体组 / 咨询组 / 全体会议` 形式回流
  - `cls industry_data / general_fast_news` 更适合降层，不适合删除

### 2026-04-15
- Task-ID:
  - `phase8-live-boundary`
  - `phase9-source-expansion`
- Task-Name:
  - `cls 并入主链 + live 头部边界收口`
- 本轮目标:
  - 把 `cls` 从 staged 推进到 `--source all`
  - 保留全球快讯覆盖，不再按旧 A 股单线目标压海外内容
  - 持续清掉当天 live 里的交易所材料噪音，并把标准交接文件更新到最新
- 已完成:
  - 新增 `cls` collector，并通过 `__NEXT_DATA__ -> telegraphList` 落地单源抓取
  - `cls` 已启用到 `configs/sources.yaml`，并进入 `--source all`
  - 收住 `cls` 相关边界：
    - `向上触及 / 向下触及` -> `market_move`
    - `隔夜全球要闻 / 新闻精选 / 你需要知道` -> `general_fast_news`
    - `general_fast_news` 强度降到低于 `market_move`，方向改为 `neutral`
    - `audit-suspicious` 不再误打 `cls` 的栏目稿和 `general_fast_news + themes`
  - 主链继续清掉一批交易所材料噪音：
    - `解除限售期解锁暨限制性股票上市公告`
    - `新签合同情况公告`
    - `注销股票期权 / 授予限制性股票 / 相关规定的核查意见`
    - `重整投资协议`
  - `申请撤销公司股票退市风险警示` 已修正为 `bullish`
  - 已更新交接文件：
    - `progress.md`
    - `task_plan.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - 还没进入“头部保留项展示分层”阶段
  - `hkex` 仍未重新进入扩源验收
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_live_smoke.py tests/test_cli_smoke.py tests/test_reference_data.py -q` -> `6 passed`
  - `./.venv/bin/python -m pytest tests/test_cls_collector.py -q` -> `3 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=1298 normalized_news=1298 events=287 analyses=287 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> `suspicious_count=0`
- 下一步:
  - 先跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
  - 再跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 若头部仍主要是 `control_change / cls general_fast_news / delisting_risk revocation`，先判断是否要做 subtype/强度/展示分层
  - 若切回扩源，再做 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment collect --source hkex`
- 新增/更新的避坑记录:
  - `cls` 已经是主链源，不要再把它当 staged 重复收口
  - 全球快讯覆盖是当前目标的一部分，不要默认把海外/栏目稿重新压掉

### 2026-04-10
- Task-ID:
  - `phase9-source-expansion`
- Task-Name:
  - `第一批官方新闻源扩展`
- 本轮目标:
  - 把扩源底座收成注册表
  - 验证 `csrc` 是否能作为第一条官方政策源接入
  - 保持 `sse` 为 staged source，不拖坏 `--source all`
- 已完成:
  - 将 collector 接入从 `cli.py` 的硬编码分支切到注册表
  - 新增 `sse` collector 骨架，并保持 `enabled: false`
  - 新增 `csrc` collector，并改为抓证监会首页 `新闻发布 -> 证监会要闻` 模块
  - `csrc` 已完成定向测试和单源真实网络验收
- 未完成:
  - `live-smoke --source all` 对 `csrc` 的全链路验收
  - `sse` 官方查询参数验通
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_csrc_collector.py -q` -> `5 passed`
  - `./.venv/bin/python -m pytest tests/test_cli_smoke.py tests/test_live_smoke.py tests/test_report_pipeline.py -q` -> `7 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment collect --source csrc` -> 成功写出 `5` 条样本
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> 用户手动中断，不能视为已验收
- 下一步:
  - 先跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
  - 再跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 若 `csrc` 不稳，先把 `enabled` 临时切回 `false`
- 新增/更新的避坑记录:
  - `csrc` 不要再抓 `c100028/common_xq_list.shtml`
  - `sse` 在真实参数未验通前不要进 `--source all`

### 2026-04-11（续）
- Task-ID:
  - `phase8-live-boundary`
  - `phase9-source-expansion`
- Task-Name:
  - `szse 启用收口 + hkex staged 接入`
- 本轮目标:
  - 把 `szse` 从 staged 推进到主链稳定状态
  - 在不拉低现有 report 信噪比的前提下，为 `hkex` 做 staged 接入和单源验收
  - 更新标准交接文件，统一最新基线和下一步优先级
- 已完成:
  - `szse` 已纳入 `--source all`，并补齐定向测试和主链回归
  - `live-smoke --source all` 已跑通，当前全链路基线更新为 `raw_news=785 normalized_news=785 events=210 analyses=210 failed_sources=none`
  - `audit-suspicious --limit 20` 已回到 `suspicious_count=0`
  - `hkex` collector 已完成官方路径摸底、定向测试和单源真实网络验收
  - 标准交接文件已补齐 `szse 已启用 / hkex staged / 最新验证基线 / 下一步动作`
- 未完成:
  - `hkex` 的最小 report 过滤尚未开始
  - `hkex` 还没有并入 `--source all`
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_szse_collector.py tests/test_cli_smoke.py tests/test_live_smoke.py tests/test_report_pipeline.py -q` -> `12 passed`
  - `./.venv/bin/python -m pytest tests/test_hkex_collector.py -q` -> `5 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=785 normalized_news=785 events=210 analyses=210 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20` -> `suspicious_count=0`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment collect --source hkex` -> 成功写出 `392` 条样本
- 下一步:
  - 先跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment collect --source hkex`
  - 只读扫描 `hkex` 最近标题，归并高噪音标题族
  - 再补最窄 report 过滤，最后才决定是否启用 `hkex`
- 新增/更新的避坑记录:
  - `hkex` 不要直接抓 `lci.html` 表格外壳
  - `hkex` 的真实数据源是官方 `lci.js` 指向的 `/ncms/json/eds/` JSON 分页
  - `report` 过滤和 `audit-suspicious` 不是同一层逻辑，验收时必须同时看两边

### 2026-04-13
- Task-ID:
  - `phase8-live-boundary`
- Task-Name:
  - `live 样本边界收口`
- 本轮目标:
  - 持续按 live 样本清理 report 头部的低信号公告
  - 把 `audit-suspicious` 压回低位，避免材料型公告误报
  - 不扩题材库，只做最窄规则和 subtype 修正
- 已完成:
  - 在 `report` 层清掉多批低信号披露：
    - `hkex` 治理/材料标题族
    - `销售情况简报 / 获得房地产项目 / 谅解备忘录 / 回购贷款承诺函`
    - `回购实施结果 / 回购用途并注销 / 互动易制度 / IPO 投资风险特别公告`
    - `减持结果 / 减持比例触及整数倍 / 预披露 / 实施完成 / 期限届满暨实施情况`
    - `行政处罚决定书 / 监管措施或处罚及整改情况`
    - `可转债信用评级报告 / 询价转让股份核查报告`
    - `基金经理配置评论 / 券商策略评论`
  - 在 `event_merge/core.py` 修正 subtype：
    - `其他风险警示` -> `delisting_risk`
    - `庭外重组` -> `reorganization_risk`
    - `授权许可协议` -> `cooperation_agreement`
  - 在 `audit-suspicious` 清掉材料误报：
    - `可转债审核问询函之回复（修订稿）`
- 未完成:
  - `冻结 / 解除冻结` 主线尚未处理
  - `风险提示公告 / 异常波动暨风险提示 / 实际控制人减持计划期限届满暨实施情况` 仍有残余变体
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -k "inquiry_transfer_share_verification_report or convertible_bond_credit_rating_report or penalty_and_rectification_disclosures or shareholder_reduction_predisclosure_and_completion_variants" -q` -> `4 passed`
  - `./.venv/bin/python -m pytest tests/test_audit_suspicious.py -k "convertible_bond_inquiry_reply_revision or cninfo_restructuring_material_reply or exchange_inquiry_reply_and_special_explanation or exchange_major_litigation_and_filing_progress_notices" -q` -> `4 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> 最近两次分别为：
    - `raw_news=624 normalized_news=624 events=186 analyses=186 failed_sources=none`
    - `raw_news=429 normalized_news=429 events=160 analyses=160 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20` -> 最近结果滚动为：
    - `suspicious_count=2`（冻结主线）
    - `suspicious_count=0`
- 下一步:
  - 先跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
  - 再跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20`
  - 优先只读判断并处理：
    - `龙元建设...部分银行账户被冻结`
    - `甘咨询...募集资金专户部分资金被冻结`
    - `深圳市龙图光罩...部分自有资金及募集资金解除冻结`
- 新增/更新的避坑记录:
  - 不要沿用上一轮 report 头部清单直接下规则；live 样本日期切换后，头部会换簇
  - `冻结 / 解除冻结` 不能自动视为材料公告；先判断是否是实质风险

### 2026-04-14
- Task-ID:
  - `phase8-live-boundary`
  - `phase9-source-expansion`
- Task-Name:
  - `live 样本边界收口 + 四批提交与远端推送`
- 本轮目标:
  - 把本地未提交改动按可提交语义拆批
  - 继续清理 `live` 样本里的低信号/误分类边界
  - 完成本地提交、远端建仓和分支推送
- 已完成:
  - 收紧并验证多组 `live` 边界：
    - `交易所低信号披露` 变体
    - `合作框架协议` 模板标题
    - `区域统计口径 + 多赛道罗列` 的多题材误挂
    - `申请破产清算` -> `reorganization_risk / bearish`
  - 完成 4 批提交：
    - `0b496f8` `fix: tighten live boundary filtering and risk subtype classification`
    - `def0907` `feat: add official source collectors and suspicious audit workflow`
    - `4948492` `feat: expand narrow theme seeds and reference mappings`
    - `eeb15de` `docs: refresh worktree handoff and task registry`
  - 创建远端仓库：
    - `https://github.com/liutao773680119-cmyk/news-sentiment-analysis`
  - 推送分支：
    - `mvp-foundation -> origin/mvp-foundation`
- 未完成:
  - `hkex` staged 过滤与并入 `--source all` 的主链验收
  - `triggered` 队列里的 `sse/cninfo` 模板材料公告尚未进一步上游收紧
  - 还没有默认分支合并策略、PR 或 release 流程
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_analysis_rules.py tests/test_analysis_scoring.py tests/test_event_merge.py tests/test_text_report_sorting.py -q` -> `233 passed`
  - `./.venv/bin/python -m pytest tests/test_cli_smoke.py tests/test_live_smoke.py tests/test_report_pipeline.py tests/test_csrc_collector.py tests/test_hkex_collector.py tests/test_sse_collector.py tests/test_szse_collector.py tests/test_miit_collector.py tests/test_audit_suspicious.py -q` -> `43 passed`
  - `./.venv/bin/python -m pytest tests/test_mapping_and_history.py tests/test_reference_data.py -q` -> `41 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> `raw_news=719 normalized_news=719 events=183 analyses=183 failed_sources=none`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20` -> `suspicious_count=0`
  - `gh repo create liutao773680119-cmyk/news-sentiment-analysis --public --description 'A-share news-driven theme radar and sentiment analysis project.'` -> 成功建仓
  - `git -C .worktrees/mvp-foundation push -u origin mvp-foundation` -> 推送成功并建立 upstream
- 下一步:
  - 先跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
  - 再跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 20`
  - 若主线继续做扩源，优先 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment collect --source hkex`
  - 若主线继续做规则，优先处理 `triggered` 队列里仍未上游压住的 `sse/cninfo` 模板材料公告
- 新增/更新的避坑记录:
  - 推送前不要假定远端仓库已存在；先 `gh repo view`，不存在再 `gh repo create`
  - `申请破产清算` 不能停留在 `corporate_disclosure / neutral`；应回正到 `reorganization_risk / bearish`

### 2026-04-14（续）
- Task-ID:
  - `phase8-live-boundary`
- Task-Name:
  - `live 样本边界收口`
- 本轮目标:
  - 按当天 `live-smoke` 头部样本继续收紧 `report` 层低信号交易所披露
  - 保持 `audit-suspicious=0`
  - 做一次标准交接，更新 worktree 交接文件
- 已完成:
  - 在 `report` 层继续清掉当天 live 冒出的低信号披露变体：
    - `责任保险`
    - `国债逆回购 / 结构性存款 / 中短期低风险金融理财产品`
    - `减持计划预披露 / 减持计划完成`
    - `股权激励法律意见 / 核查意见 / 注销 / 回购注销 / 自查报告`
    - `持续督导意见 / 受让协议`
    - `中标项目签订协议的进展公告`
    - `模板框架协议`
    - `miit` 的 `巡视动员部署会`
  - 为上述变体新增多组 `tests/test_text_report_sorting.py` 红灯测试，并全部转绿
  - 完成标准交接文件更新：
    - `progress.md`
    - `task_registry.md`
    - `修改记录_会话备忘.md`
    - `避坑记录.md`
- 未完成:
  - `hkex` staged 过滤与并入 `--source all` 的主链验收仍未开始
  - 当前头部剩余 `盈新发展...控制权变更`、`文科股份...联合中标` 两条，还没判断是否要上游修 subtype/analysis
- 关键命令/结果:
  - `./.venv/bin/python -m pytest tests/test_text_report_sorting.py -q` -> `116 passed`
  - `./.venv/bin/python -m pytest tests/test_report_pipeline.py -q` -> `3 passed`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10` -> 多次复跑均为 `suspicious_count=0`
  - `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all` -> 本轮滚动结果包括：
    - `raw_news=1046 normalized_news=1046 events=236 analyses=236 failed_sources=none`
    - `raw_news=695 normalized_news=695 events=203 analyses=203 failed_sources=none`
    - `raw_news=1046 normalized_news=1046 events=236 analyses=236 failed_sources=none`
- 下一步:
  - 先跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment live-smoke --source all`
  - 再跑 `PYTHONPATH=src ./.venv/bin/python -m news_sentiment audit-suspicious --limit 10`
  - 若头部仍是 `盈新发展...控制权变更`、`文科股份...联合中标` 一类，优先判断是否需要改 `event_subtype / analysis`，不要继续堆 report 过滤
  - 若主线切回扩源，再按原计划进入 `hkex`
- 新增/更新的避坑记录:
  - 连续清掉交易所材料噪音后，如果头部剩下的是 `control_change / order_contract` 真催化候选，先看上游分类，不要继续在 report 层硬压
